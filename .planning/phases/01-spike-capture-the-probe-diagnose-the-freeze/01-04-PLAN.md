---
phase: 01-spike-capture-the-probe-diagnose-the-freeze
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VERDICT.md
  - package.json
autonomous: false
requirements: [SPIKE-03, SPIKE-04]
user_setup: []
must_haves:
  truths:
    - "01-VERDICT.md states GREEN or RED explicitly and whether Phase 4 activates"
    - "A GREEN verdict is gated on the D-09 byte-match (a candidate that returned raw 32 bytes matching the noble reference for a 19-byte salt), never on mere reachability"
    - "The verdict records the completed sync-derive import-graph audit (core/stegcloak.ts not value-imported in src/) and cites the permanent build guard that keeps it true"
    - "The freeze root cause is written with on-device evidence (yield count, first-yield/longest-block ms, LOAD_MESSAGES_SUCCESS storm concurrency, nextTick macrotask-form result)"
    - "The verdict marks the non-tested platform as untested/assume-RED and records the tested device's OS + Discord/Hermes build tag (D-10)"
  artifacts:
    - path: ".planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VERDICT.md"
      provides: "The committed GREEN/RED native-feasibility verdict + freeze root-cause writeup gating Phase 4"
      contains: "GREEN"
  key_links:
    - from: "01-VERDICT.md"
      to: "01-03-SUMMARY.md (on-device evidence)"
      via: "verdict cites the per-candidate byteMatch + freeze numbers"
      pattern: "byteMatch|byte-match"
    - from: "01-VERDICT.md verdict"
      to: "ROADMAP Phase 4 activation"
      via: "GREEN ⇒ Phase 4 runs; RED ⇒ milestone ships on Phase 3 alone"
      pattern: "Phase 4"
---

<objective>
Author and commit the spike's terminal deliverable: a written GREEN/RED native-feasibility verdict (SPIKE-04) and the evidence-backed first-encrypt freeze root cause (SPIKE-03 writeup), driven by the on-device evidence captured in Plan 03. The verdict gates whether Phase 4 (the conditional native fast-path) ever runs. Also land the one hygiene action the spike's findings recommend: pin `@noble/hashes` exactly (drop the `^` caret) so a minor bump cannot silently regress the build-time macrotask patch.

Purpose: The milestone is shaped SPIKE → GATE → FALLBACKS precisely so the native decision is made from durable evidence, not assumption. This plan converts the captured evidence into a committed decision. GREEN is gated strictly on the D-09 byte-match (shape-pass AND 32 bytes matching the noble reference for a real 19-byte salt) — never on mere reachability — to defend against the catastrophic silent-wrong-32-bytes failure mode. The research basis expects RED, but both branches are written honestly. This is `autonomous: false` because the verdict is a human-confirmed decision built from the Plan 03 on-device evidence, and the exact-pin touches the dependency contract.

Output: `01-VERDICT.md` (committed planning artifact, Claude's-discretion location/format per D-09/D-10), and an exact-pinned `@noble/hashes` in `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-01-SUMMARY.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-02-SUMMARY.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin @noble/hashes exactly in package.json (drop the caret)</name>
  <files>package.json</files>
  <read_first>
    - package.json (the `@noble/hashes` dependency line — currently `^1.7.1`)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md § "Standard Stack" + "Pitfall 2" (the caret risk: package.json ^1.7.1, lockfile 1.8.0, npm latest 2.2.0 — a bump reverts nextTick to the microtask form and silently re-freezes)
    - scripts/build.mjs (lines 29-42 — the build-time nextTick macrotask patch the pin protects)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-01-SUMMARY.md (the nextTick-in-test-bundle finding)
  </read_first>
  <action>
    In `package.json`, change the `@noble/hashes` dependency from the caret range `^1.7.1` to the EXACT lockfile-resolved version `1.8.0` (drop the `^`). This is the hygiene action the spike's findings recommend (RESEARCH § Pitfall 2 / D-06): a caret re-resolve could ship a noble where the patched `export const nextTick = async () => { };` line changed, reverting `nextTick` to the microtask form so "async" derivation never yields the renderer — a silent re-freeze. The runtime `assertMacrotaskYield()` (Plan 02) is the tripwire; this pin removes the risk at the source. Run `npm install` to confirm the lockfile still resolves `1.8.0` and is consistent, then `npm test && npm run build` to confirm nothing regressed. PERF-02 (Phase 3) formally owns this, but landing the one-line pin here closes the spike's most actionable finding immediately.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json');const v=(p.dependencies||{})['@noble/hashes'];process.exit(v==='1.8.0'?0:1)" && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `@noble/hashes` as exactly `1.8.0` (no `^`).
    - `npm test` exits 0 (byte-compat harness + the nextTick macrotask-form assertion green — the pin keeps the macrotask patch in effect).
    - `npm run build` exits 0 (the build-time nextTick patch still applies; no regression).
  </acceptance_criteria>
  <done>@noble/hashes is pinned exactly to 1.8.0; harness + build green; the caret re-freeze risk is closed at the source.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Author the GREEN/RED verdict + freeze root cause, gated on the D-09 byte-match (SPIKE-04 / SPIKE-03)</name>
  <files>.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VERDICT.md</files>
  <read_first>
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-03-SUMMARY.md (the on-device evidence: per-candidate {reachable, saltAccepted, outputKind, byteMatch, crashed, timingMs} or `none reachable`; restart-survival; armed-flag A1 finding; build-tag A5 finding; freeze yield/timing/storm numbers)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-02-SUMMARY.md (the live-path confirmation that flux.ts/send.ts import the ASYNC keycache deriveKey; the instrumentation shape)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-01-SUMMARY.md (the committed D-09 reference vector; the build-guard fire/revert demonstration)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md § "The Verdict Decision Tree" / "Validation Architecture" / "Completed Audit" + the RED-expected basis
    - .planning/ROADMAP.md § Phase 4 (what a GREEN verdict unlocks) and the SPIKE→GATE→FALLBACKS shape
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-CONTEXT.md (D-09 GREEN bar, D-10 device coverage, the Claude's-discretion verdict-location note)
  </read_first>
  <what-built>
    All spike machinery is in place and exercised on-device: the persisted ProbeReport, the crash-safe candidate test with the D-09 byte-match, the real-path freeze instrumentation, the permanent sync-derive build guard, and the exact @noble/hashes pin. The on-device evidence is captured in 01-03-SUMMARY.md.
  </what-built>
  <action>
    CHECKPOINT (human-verify): author `.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VERDICT.md` per <how-to-verify>, citing the on-device evidence from 01-03-SUMMARY.md (do NOT invent numbers). State GREEN or RED explicitly with the Phase-4 activation line; permit GREEN ONLY when a candidate passed the D-09 byte-match (reachable + 19-byte salt accepted + raw 32 bytes + bytes match the noble reference) — never on reachability alone. Record the per-candidate table (or `none reachable`), device coverage (D-10), the evidence-backed freeze root cause, the recorded sync-derive audit + build guard (D-07), and the A1/A5 findings. Reject any GREEN verdict lacking a passing byteMatch.
  </action>
  <how-to-verify>
    Author `.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VERDICT.md` from the Plan 03 evidence (do NOT invent numbers — cite the captured values). It MUST contain:
    1. **Native-feasibility verdict: GREEN or RED, stated explicitly**, with the Phase-4 activation line ("Phase 4 activates" / "Phase 4 does NOT run; milestone ships on Phase 3 alone"). GREEN is permitted ONLY if a candidate was reachable AND accepted the real 19-byte salt AND returned a raw 32-byte key AND those 32 bytes byte-matched the noble reference (D-09). Anything short of all four ⇒ RED. Reachability alone is NEVER GREEN.
    2. **Per-candidate evidence table** (or `none reachable`): name, reachable, salt-accepted (19-byte), output-kind (raw32/phc/other), byteMatch, timingMs, crashed.
    3. **Device coverage (D-10):** tested device OS + Discord/Hermes build tag; the other platform explicitly marked "untested — treated as RED until probed."
    4. **Freeze root cause (SPIKE-03), evidence-backed:** the recorded yield-sample count (zero ⇒ thread-starved), first-yield/longest-block ms, bench total, the LOAD_MESSAGES_SUCCESS storm concurrency count, and the nextTick macrotask-form result. State which of the candidate causes (nextTick regression / coarse asyncTick:50 / LOAD_MESSAGES_SUCCESS storm / front-loaded first pass) the evidence implicates — Phase 3 owns the FIX; this records the diagnosis.
    5. **Sync-derive audit record (D-07):** state the completed import-graph finding (core/stegcloak.ts is NOT value-imported anywhere in src/; flux.ts/send.ts use the async keycache deriveKey) and cite the permanent build.mjs guard (with the fire/revert demonstration) that keeps it true.
    6. **A1 storage-flush + A5 build-tag findings** from the on-device test.
    Then confirm the verdict is internally consistent (a GREEN verdict with no passing byteMatch is INVALID — reject and correct).
  </how-to-verify>
  <resume-signal>Confirm the verdict reads GREEN or RED with the Phase-4 line, the GREEN gate is the D-09 byte-match (not reachability), and all evidence is cited from 01-03-SUMMARY.md — or describe corrections needed.</resume-signal>
  <verify>
    <human-check>01-VERDICT.md states GREEN/RED + Phase-4 activation; GREEN (if any) is justified by a passing D-09 byte-match, not reachability; freeze root cause + sync-derive audit + device coverage + A1/A5 findings are all recorded from real evidence.</human-check>
  </verify>
  <done>A committed 01-VERDICT.md states the native-feasibility verdict (GREEN/RED) gated on the D-09 byte-match, records the evidence-backed freeze root cause and the sync-derive audit, marks device coverage per D-10, and explicitly gates Phase 4.</done>
</task>

</tasks>

<verification>
- `01-VERDICT.md` exists, states GREEN or RED explicitly, and gates Phase 4.
- A GREEN verdict (if any) cites a passing D-09 byte-match; a RED verdict cites the failing/absent byte-match or unreachability.
- The freeze root cause is written from captured on-device numbers; the sync-derive audit + build guard are recorded.
- `package.json` pins `@noble/hashes` at exactly `1.8.0`; `npm test` + `npm run build` green.
</verification>

<success_criteria>
- The written native-feasibility verdict (GREEN/RED) is committed and explicitly states whether Phase 4 activates (SPIKE-04 / D-09 / D-10).
- The first-encrypt freeze has a written, evidence-backed root cause incorporating the yield count, nextTick macrotask assertion, the recorded sync-derive audit + build guard, and the LOAD_MESSAGES_SUCCESS storm observation (SPIKE-03).
- The caret re-freeze risk is closed by the exact @noble/hashes pin.
</success_criteria>

<output>
Create `.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-04-SUMMARY.md` when done. Record: the final verdict (GREEN/RED), the Phase-4 activation decision, the freeze root cause as written, and confirmation the @noble/hashes pin landed.
</output>
