---
phase: 01-spike-capture-the-probe-diagnose-the-freeze
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/crypto/argon.ts
  - src/discord/flux.ts
  - src/discord/send.ts
autonomous: true
requirements: [SPIKE-03]
must_haves:
  truths:
    - "assertMacrotaskYield() returns ok:false when noble's nextTick is the empty-async-arrow microtask form and ok:true when it is the setTimeout macrotask form"
    - "The instrumented derive wrapper early-returns the plain deriveKeyAsync with zero overhead when the debug flag (passed in by parameter) is false — no crypto→settings up-graph import"
    - "When the debug flag is true, the wrapper returns wall-time totalMs, firstYieldMs, and a macrotask-fired sample count gathered by a setInterval(0) sampler"
    - "flux.ts and send.ts expose a debug-gated observation of the LOAD_MESSAGES_SUCCESS storm (concurrent backgroundDecrypt launches) and the cold-path reject, gated so normal use has zero overhead"
  artifacts:
    - path: "src/crypto/argon.ts"
      provides: "assertMacrotaskYield(), a parameter-gated instrumented derive wrapper, enriched bench timing"
      contains: "assertMacrotaskYield"
    - path: "src/discord/flux.ts"
      provides: "debug-gated concurrency counter on backgroundDecrypt / LOAD_MESSAGES_SUCCESS"
      contains: "deriving"
    - path: "src/discord/send.ts"
      provides: "debug-gated cold-path observation (no behavior change)"
      contains: "getCachedKey"
  key_links:
    - from: "src/crypto/argon.ts assertMacrotaskYield"
      to: "@noble/hashes/utils nextTick"
      via: "String(nextTick) runtime form assertion"
      pattern: "nextTick"
    - from: "src/discord/flux.ts"
      to: "concurrency counter"
      via: "increment on deriving.add, decrement in .finally (debug-gated)"
      pattern: "deriving"
---

<objective>
Instrument the REAL async derivation + dispatch path so the first-encrypt freeze has measured, not assumed, evidence (D-06a). Add the runtime `nextTick.toString()` macrotask assertion (the caret-regression on-device tripwire), a debug-flagged yield/timing sampler on the real `deriveKeyAsync`, enriched bench timing, and zero-overhead debug-gated observation of the `LOAD_MESSAGES_SUCCESS` concurrency storm and the cold-path send reject.

Purpose: SPIKE-03 requires evidence from the real path — the storm only manifests on the live dispatch path, and the macrotask-yield assertion is the on-device counterpart to the Plan 01 CI assertion. Per D-08 the instrumentation is kept behind a debug flag as the seed of Phase-3 OBS-01/OBS-02, so Phase 3 hardens rather than recreates. CRITICAL layering constraint (PATTERNS.md flag): `argon.ts` is crypto-layer and MUST NOT import `settings()` — the debug flag is injected BY PARAMETER (mirroring `random.ts`'s `rng: RandomBytes` DI), never read inside crypto.

Output: Instrumented `src/crypto/argon.ts` (new exported helpers, existing `deriveKeyAsync`/`benchOnce`/`OPTS` byte-compat params untouched), debug-gated observation hooks in `src/discord/flux.ts` and `src/discord/send.ts`. No behavior change to the freeze itself (the FIX is Phase 3) — observe and measure only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VALIDATION.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add assertMacrotaskYield() + a parameter-gated instrumented derive wrapper to argon.ts</name>
  <files>src/crypto/argon.ts</files>
  <read_first>
    - src/crypto/argon.ts (the file being modified — OPTS at lines 18-24, ASYNC_OPTS asyncTick:50 at line 38, deriveKeyAsync at 40-42, benchOnce at 45-49; the byte-compat params t:3,m:65536,p:1,version:0x13,dkLen:32 must NOT change)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md § "src/crypto/argon.ts" (lines 90-112 — the instrumentation sketch AND the HARD layering flag: prefer parameter injection over settings() inside argon.ts)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md § "Pattern 3" (lines 306-339 — the setInterval(0) sampler, the honest A4 caveat that noble's internal yield count is not directly observable, the nextTick.toString microtask regex). NOTE: the code SKETCH at ~lines 324-326 deliberately shows the WRONG `settings().debugInstrument` approach — see the DISREGARD instruction in this task's action; do not copy that line.
    - src/crypto/random.ts (lines 21-40, 102-110 — the canonical DI-by-parameter precedent: rng injected as a parameter, not read from settings; mirror this for the debug flag)
    - scripts/build.mjs (lines 29-42 — the build-time nextTick macrotask patch this assertion verifies held)
  </read_first>
  <behavior>
    - assertMacrotaskYield() returns { ok: false, form } when String(nextTick) matches the empty-async-arrow form `/async\s*\(\s*\)\s*=>\s*\{\s*\}/` (microtask = regressed), and { ok: true, form } otherwise (macrotask setTimeout form).
    - deriveKeyAsyncInstrumented(password, channelId, debug=false): when debug is false, awaits and returns the plain deriveKeyAsync result with no sampler started (zero overhead).
    - When debug is true, it returns an object exposing the derived key plus totalMs (wall time), firstYieldMs (first setInterval(0) sample), yieldSamples (count of macrotasks that fired during derivation), and the assertMacrotaskYield() result. Zero yieldSamples during a multi-second derivation indicates a thread-starved (effectively frozen) UI.
  </behavior>
  <action>
    Edit `src/crypto/argon.ts`. Add `import { nextTick } from "@noble/hashes/utils"` (the patched symbol).
    Export `function assertMacrotaskYield(): { ok: boolean; form: string }` that computes `const src = String(nextTick)` and returns `{ ok: !/async\s*\(\s*\)\s*=>\s*\{\s*\}/.test(src), form: src.slice(0, 80) }`. This is the on-device runtime tripwire for the @noble/hashes caret regression (package.json `^1.7.1` resolves 1.8.0 in lockfile; a bump to 2.x would revert nextTick to the microtask form and silently re-freeze).
    Export `async function deriveKeyAsyncInstrumented(password: string, channelId: string, debug = false)`. CRITICAL: the `debug` flag is a PARAMETER — do NOT import or call `settings()` inside argon.ts (that would create a forbidden crypto→settings up-graph edge; mirror random.ts's `rng: RandomBytes` parameter-injection convention). DISREGARD the `settings().debugInstrument` usage shown in RESEARCH.md Pattern 3 (~lines 324-326) — that sketch is the flagged wrong approach (PATTERNS.md flag #1, the exact crypto→settings up-graph edge it forbids). Use parameter injection: `async function deriveKeyAsyncInstrumented(password, channelId, debug = false)` where `debug` is a plain boolean parameter, mirroring random.ts's `rng: RandomBytes` DI. When `!debug`, `return deriveKeyAsync(password, channelId)` immediately. When `debug`, record `t0 = Date.now()`, start a `setInterval(() => { const t = Date.now() - t0; if (firstYield < 0) firstYield = t; samples.push(t); }, 0)` sampler, `await deriveKeyAsync(...)`, then in a `finally` `clearInterval(id)`, and return `{ key, totalMs, firstYieldMs: firstYield, yieldSamples: samples.length, ...assertMacrotaskYield() }`. Document the A4 caveat in a comment: noble's internal `await nextTick()` count is not directly observable from outside the closure; the sampler proves whether macrotasks fire (UI alive) vs zero samples (thread starved).
    Do NOT change `OPTS`, `ASYNC_OPTS`, `deriveKey`, or `deriveKeyAsync` — they encode the GoofCord-compat params and the existing async path. Use array index loops only; no `class`; no generators.
  </action>
  <verify>
    <automated>grep -q "export function assertMacrotaskYield" src/crypto/argon.ts && grep -q "deriveKeyAsyncInstrumented" src/crypto/argon.ts && ! grep -q 'settings()' src/crypto/argon.ts && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/crypto/argon.ts` exports `assertMacrotaskYield` and `deriveKeyAsyncInstrumented`.
    - `src/crypto/argon.ts` contains NO call to `settings()` (`! grep -q 'settings()' src/crypto/argon.ts` succeeds) — the debug flag is parameter-injected, preserving the discord→core→crypto layering.
    - `OPTS` still reads `t: 3`, `m: 65536`, `p: 1`, `version: 0x13`, `dkLen: KEY_LENGTH` (byte-compat params unchanged).
    - `npm run build` exits 0 and `npm test` exits 0 (byte-compat harness still green; nextTick CI assertion from Plan 01 still passes).
  </acceptance_criteria>
  <done>assertMacrotaskYield + the parameter-gated instrumented wrapper exist; argon.ts imports no settings; byte-compat params untouched; build + harness green.</done>
</task>

<task type="auto">
  <name>Task 2: Enrich benchOnce timing + add debug-gated storm/cold-path observation to flux.ts and send.ts</name>
  <files>src/crypto/argon.ts, src/discord/flux.ts, src/discord/send.ts</files>
  <read_first>
    - src/crypto/argon.ts (benchOnce at lines 45-49 — the existing total-ms timer to enrich)
    - src/discord/flux.ts (the file being modified — backgroundDecrypt at 40-72, the deriving Set guard at 17, the LOAD_MESSAGES_SUCCESS loop at 80-84; deriveKey here is the ASYNC keycache one, NOT the sync argon one)
    - src/discord/send.ts (the file being modified — the cold-path reject block at 47-62, fail()/noteError conventions at 25-29)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md § "src/discord/flux.ts" (lines 116-130) and § "src/discord/send.ts" (lines 134-148) — observe only, no behavior change, debug-gated, never throw inside the Flux hook
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md § "Pitfall 3" (the LOAD_MESSAGES_SUCCESS concurrency storm — N coroutines guarded per-messageId not per-channel; the fix is Phase 3)
    - src/core/health.ts (noteError counters — the accumulate-don't-throw convention; reuse shape for any counter)
    - src/settings.ts (the debugInstrument field from Plan 01 — read it in the discord layer, which MAY import settings)
  </read_first>
  <action>
    Enrich the bench readout in `src/crypto/argon.ts` and LOCK its return contract here (this is the contract Plan 03 Task 3 binds against — do NOT defer the shape to the SUMMARY). Add a sibling `export async function benchOnceDetailed(): Promise<{ totalMs: number; firstYieldMs: number; longestBlockMs: number; yieldCount: number; ok: boolean; form: string }>` that preserves the existing `benchOnce(): Promise<number>` signature unchanged (so existing callers do not break). `benchOnceDetailed` runs a single derivation under the same setInterval(0) first-yield + sampling instrumentation as Task 1, computing: `totalMs` (wall time), `firstYieldMs` (first macrotask sample), `longestBlockMs` (max gap between consecutive samples ⇒ the worst UI stall), `yieldCount` (sample count; zero during a multi-second derivation ⇒ thread-starved), and spreads `...assertMacrotaskYield()` for `{ ok, form }`. Still NO `settings()` import in argon.ts — `benchOnceDetailed` takes no settings, it just measures. Record the chosen field names in the SUMMARY for traceability, but the contract above is authoritative.
    In `src/discord/flux.ts`: add a debug-gated concurrency observation of the storm. Gate ALL new observation on `settings().debugInstrument` (discord layer MAY import settings — the up-graph rule only forbids crypto/core importing settings). Maintain a module-level `let activeDerivations = 0` counter; in `backgroundDecrypt`, when `settings().debugInstrument`, increment on `deriving.add(id)` and decrement in the existing `.finally`, and log peak concurrency via `vendetta.logger.log` (never throw inside the Flux hook — accumulate, mirror the noteError convention). Do NOT change the dispatch behavior, the per-messageId guard, or the re-dispatch — observe only; the per-channel guard FIX is Phase 3.
    In `src/discord/send.ts`: add a debug-gated observation of the cold-path. When `settings().debugInstrument`, on the cache-miss branch (lines 58-62) record a timestamp before `deriveKey(...)` and log first-key-ready latency in the `.then`. Do NOT change the reject-and-resend behavior. Use the existing `fail()`/`noteError`/`showToast` surfaces.
    Confirm: the `deriveKey` imported in both files is `core/keycache`'s ASYNC variant (the completed audit; the send/flux paths already take the async route) — record this confirmation in the SUMMARY as the live-path half of the SPIKE-03 sync-leak evidence.
  </action>
  <verify>
    <automated>grep -q "benchOnceDetailed" src/crypto/argon.ts && grep -q "debugInstrument" src/discord/flux.ts && grep -q "debugInstrument" src/discord/send.ts && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/crypto/argon.ts` exports `benchOnceDetailed` returning the LOCKED shape `{ totalMs, firstYieldMs, longestBlockMs, yieldCount, ok, form }`, with the existing `benchOnce(): Promise<number>` signature untouched and no `settings()` import in argon.ts.
    - `src/discord/flux.ts` references `settings().debugInstrument` to gate a concurrency counter that increments on `deriving.add` and decrements in `.finally`; the existing dispatch/decrypt behavior is unchanged.
    - `src/discord/send.ts` references `settings().debugInstrument` to gate cold-path latency logging; the reject-and-resend behavior is unchanged.
    - `npm run build` exits 0 and `npm test` exits 0 (no class/generator/iterator violations; byte-compat harness green).
  </acceptance_criteria>
  <done>benchOnceDetailed returns the locked `{ totalMs, firstYieldMs, longestBlockMs, yieldCount, ok, form }` contract; flux/send carry zero-overhead debug-gated observation of the storm and cold path with no behavior change; both confirm the async-route imports; build + harness green.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (instrumentation survives swc ES5 + the class/generator/iterator + sync-derive guards).
- `npm test` green (byte-compat harness + Plan 01 nextTick/round-trip/D-09 assertions) — COMPAT-01 held.
- `src/crypto/argon.ts` imports no `settings()` (layering preserved; debug flag parameter-injected).
- Behavior of the freeze path is unchanged (observe-only; the fix is Phase 3).
</verification>

<success_criteria>
- The on-device macrotask-yield tripwire (`assertMacrotaskYield`) exists and is consumable by the diag/bench readouts (Plan 03) (SPIKE-03).
- The real `deriveKeyAsync` and the dispatch storm are instrumented behind a zero-overhead debug flag, parameter-injected to preserve layering (SPIKE-03 / D-06a / D-08).
- `benchOnceDetailed` exposes a locked enriched-timing contract `{ totalMs, firstYieldMs, longestBlockMs, yieldCount, ok, form }` so Plan 03's bench wires against a known shape.
- The instrumentation is structured to seed Phase-3 OBS-01/OBS-02 without recreating it.
</success_criteria>

<output>
Create `.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-02-SUMMARY.md` when done. Record: the exact instrumented-wrapper return shape, confirmation that `benchOnceDetailed` returns the locked `{ totalMs, firstYieldMs, longestBlockMs, yieldCount, ok, form }` contract (so Plan 03 binds to it), and the confirmed finding that flux.ts/send.ts both import the ASYNC keycache deriveKey (the live-path half of the SPIKE-03 sync-leak evidence).
</output>
