---
phase: 01-spike-capture-the-probe-diagnose-the-freeze
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/discord/nativeProbe.ts
  - src/index.ts
  - src/discord/commands.ts
autonomous: false
requirements: [SPIKE-01, SPIKE-02, SPIKE-03]
user_setup: []
must_haves:
  truths:
    - "runProbe() enumerates nativeModuleProxy, __turboModuleProxy candidates, vendetta.metro.findByProps argon/pwhash/scrypt probes, and crypto.subtle into a structured ProbeReport, persisted via settings()"
    - "On load, the probe runs enumeration-only (D-03) when the stored report is missing or the build tag changed (D-02), wrapped in safe(); it never invokes native crypto on load"
    - "reconcileArmedFlag() on load marks a still-armed candidate as crashed/unsafe and clears the flag (D-05 poison detection)"
    - "testCandidate() runs ONLY via the manual /encrypt diag --test command (D-05): armed-flag set before the call, tiered cheap(m=8,t=1)-then-real(m=65536,t=3,p=1) params (D-04), byte-match vs the on-device noble deriveKey reference for a 19-byte salt (D-09), flag cleared in finally"
    - "The persisted ProbeReport renders on-device via __goofcrypt.diag() and /encrypt status after a Discord restart"
  artifacts:
    - path: "src/discord/nativeProbe.ts"
      provides: "enumerateSurface(), runProbe(), reconcileArmedFlag(), testCandidate(), probeSummary() — discord-layer probe"
      contains: "export function runProbe"
      min_lines: 80
    - path: "src/index.ts"
      provides: "onLoad probe wiring (safe + stale-gated) + __goofcrypt.diag() ProbeReport surfacing"
      contains: "nativeProbe"
    - path: "src/discord/commands.ts"
      provides: "/encrypt diag --probe / --test verb + status ProbeReport render + bench richer timing"
      contains: "diag"
  key_links:
    - from: "src/index.ts onLoad"
      to: "src/discord/nativeProbe.ts runProbe"
      via: "safe('native-probe', maybeRunProbe) — enumeration only, stale-gated"
      pattern: "nativeProbe|runProbe"
    - from: "src/discord/commands.ts /encrypt diag --test"
      to: "src/discord/nativeProbe.ts testCandidate"
      via: "the ONLY caller of candidate invocation (D-05)"
      pattern: "testCandidate|diag"
    - from: "src/discord/nativeProbe.ts byte-match"
      to: "src/crypto/argon.ts deriveKey (sync) + src/selfTest.ts eqBytes"
      via: "D-09 on-device noble reference key comparison"
      pattern: "deriveKey|eqBytes"
---

<objective>
Build the permanent `src/discord/nativeProbe.ts` (D-01) — the durable replacement for the fire-and-forget `diagnose2.txt`. It enumerates the on-device native-crypto surface into the persisted `ProbeReport` (SPIKE-01), runs crash-safe tiered candidate invocation with armed-flag poison detection ONLY on a manual command (SPIKE-02 / D-04 / D-05), byte-matches any candidate against an on-device noble reference for a real 19-byte salt (D-09), and surfaces all of it via `__goofcrypt.diag()` and `/encrypt status` (D-02). Wire the on-load enumeration (safe, stale-gated, enumeration-only) and the `/encrypt diag --probe`/`--test` command surface.

Purpose: This is the spike's central deliverable — the never-recorded probe output made durable and conclusive on the user's primary device (D-10). It settles native reachability (SPIKE-01/02) and feeds the verdict (Plan 04). It is `autonomous: false` because the conclusive evidence (probe persists across restart, armed-flag survives a hard crash, real-param candidate run) MUST be gathered by the user on-device — the Node CI runner has no native modules and no real Hermes (VALIDATION.md § Manual-Only Verifications).

Output: New `src/discord/nativeProbe.ts` (discord-layer; the ONLY new code touching `vendetta.*`/`nativeModuleProxy`/`__turboModuleProxy`/metro), wired into `src/index.ts` onLoad + diag hook, with the `diag` verb + ProbeReport render + richer bench in `src/discord/commands.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VALIDATION.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-01-SUMMARY.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-02-SUMMARY.md
@diagnose2.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build src/discord/nativeProbe.ts — enumeration, persistence, armed-flag reconcile, tiered candidate test</name>
  <files>src/discord/nativeProbe.ts</files>
  <read_first>
    - diagnose2.txt (the exact enumeration to port: nativeModuleProxy regex /crypt|sodium|nacl|argon|pwhash|kdf|scrypt|pbkdf|dave|hash/i, the __turboModuleProxy candidate list, the metro.findByProps probe list, crypto.subtle)
    - src/crypto/random.ts (the canonical discord-adjacent module shape to MIRROR: module-level `let` singletons + functions, NO class; per-host-probe try/catch; `(globalThis as any).vendetta?.metro?.findByProps` guarded access; boolean/string accessors; the coerce() Uint8Array|number[]|base64 normalizer at lines 27-40)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md § "Pattern 1" (lines 218-262 enumerateSurface skeleton), § "Pattern 2" (lines 264-303 testCandidate + reconcileArmedFlag), § "Code Examples › Real-length salt" (lines 468-476)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md § "src/discord/nativeProbe.ts" (lines 29-86 — all analogs, the armed-flag rules, lazy re-resolution to avoid the _msgActions stale-handle bug, the D-09 eqBytes+deriveKey reuse)
    - src/settings.ts (the ProbeReport + CandidateResult interfaces and the nativeProbe/nativeProbeArmed fields from Plan 01; persist via settings() accessor ONLY — never raw plugin.storage)
    - src/crypto/argon.ts (the SYNC deriveKey at line 27 for the D-09 noble reference; OPTS real params; the cheap-params test needs m=8,t=1 — pass a separate options object, do NOT mutate OPTS)
    - src/selfTest.ts (eqBytes at lines 12-16 — reuse for the byte-match; do NOT write a new comparator)
    - src/util/base64.ts and src/crypto/deflate.ts (fromBase64 / utf8Encode — Hermes-safe encoding; no atob/Buffer/TextEncoder)
  </read_first>
  <action>
    Create `src/discord/nativeProbe.ts` as a discord-layer module mirroring `random.ts`'s shape: module-level `let` + exported functions, NO `class`, NO generators, array index loops only (NEVER `for...of` over a Map/Set — the swc iterator lowering drops the first element under Discord's Hermes). It is the ONLY new code allowed to touch `vendetta.*`/`globalThis.nativeModuleProxy`/`__turboModuleProxy`/metro; it must NOT be imported by core/ or crypto/.
    LAYER NOTE for the byte-match comparator (W1 resolution): import `eqBytes` from `../selfTest` (`src/selfTest.ts`). `selfTest.ts` is a top-level `src/` cross-cutting utility module, NOT in core/crypto/stego/util — the discord layer MAY import it as a sibling utility; this is explicitly NOT an up-graph layering violation (unlike importing `core/stegcloak`). Do NOT inline a second byte-compare; reuse `eqBytes` so the D-09 comparison matches the harness exactly.
    Implement `function enumerateSurface(): ProbeReport` porting `diagnose2.txt`: scan `(globalThis as any).nativeModuleProxy` keys filtered by `/crypt|sodium|nacl|argon|pwhash|kdf|scrypt|pbkdf|dave|hash/i` → `cryptoIsh`; call `(globalThis as any).__turboModuleProxy` with the candidate list `["NativeCryptoModule","DCDCrypto","Sodium","NativeSodium","RNSodium","DCDSodiumManager","NativeDAVE","DCDDAVEManager","NativeMLS","Aes"]` (falling back to `nmp[name]`) and record `Object.keys(m).slice(0,20)` per hit → `turboHits`; probe `(globalThis as any).vendetta?.metro?.findByProps` for `["crypto_pwhash","argon2id","argon2","crypto_pwhash_ALG_ARGON2ID13","scrypt","pbkdf2","subtle","secretbox"]` recording found + `Object.keys(mod).slice(0,10)` → `metroHits`; set `subtle = !!((globalThis as any).crypto && (globalThis as any).crypto.subtle)`. Wrap EVERY host access in its own `try { } catch { }` (the vendetta/native API is `any`-typed and may be absent). Record `timestamp`, `buildTag` (scan for a reachable Discord/Hermes build tag — try metro/vendetta fields; if none reliably reachable, set null and note that D-02 falls back to manual-only re-probe per Open Question 2), `platform` (D-10), `version`, and initial `verdict: "untested"`.
    Implement `function runProbe(): ProbeReport` — call enumerateSurface(), persist via `settings().nativeProbe = report` (settings() accessor only), and return it. Enumeration ONLY — never invoke native crypto here (D-03).
    Implement `function reconcileArmedFlag(report: ProbeReport): void` (D-05 poison detection): read `settings().nativeProbeArmed`; if set, mark that candidate `crashed: true` in the report (record it crashed last run, skip future tests), then clear `settings().nativeProbeArmed = null`.
    Implement `async function testCandidate(name, fn): Promise<CandidateResult>` (D-04/D-05/D-09) — the ONLY native-invoking function, called ONLY from the manual command (Task 3). Set `settings().nativeProbeArmed = name` BEFORE the call (poison flag). Run a tiered test under a `withTimeout` race (timeout for hangs) and `try/catch` (JS throws): FIRST a cheap-params probe with `{ m: 8, t: 1, p: 1, dkLen: 32, version: 0x13 }` and the real 19-byte salt `"1234567890123456789"` to learn shape (salt accepted? raw-32 vs PHC-string vs other? — write `classifyOutput()` using fromBase64/coerce for base64 returns, never atob/Buffer); ONLY if the shape passes (`outputKind === "raw32"` && `saltAccepted`), do ONE real run with the GoofCord params `{ m: 65536, t: 3, p: 1, dkLen: 32, version: 0x13 }`, capture timingMs, and byte-match its 32 bytes against the on-device noble reference `deriveKey(VEC_PW, "1234567890123456789")` (sync deriveKey from crypto/argon) using `eqBytes` from selfTest — set `byteMatch` accordingly. Clear `settings().nativeProbeArmed = null` in `finally`. Re-resolve native handles on each call — do NOT memoize across runs (avoid the `_msgActions ??=` stale-handle bug).
    Implement `function probeSummary(): string` returning a compact one-line render for `/encrypt status` (e.g. `" · probe: <verdict>, <n> candidates, <m> crashed"`), mirroring `healthSummary()`.
    Use `noteError(kind, e)` for silent failures. Catch params: `(e as Error)?.message ?? String(e)`.
    Open Question A1/Pitfall 4 — the armed-flag write must reach disk BEFORE the native call for crash detection to work. This is a MEDIUM-confidence assumption about Kettu storage flush timing; the on-device test (Task 4 of this plan, manual) verifies it. Add a code comment marking this as a spike finding to confirm.
  </action>
  <verify>
    <automated>grep -q "export function runProbe" src/discord/nativeProbe.ts && grep -q "reconcileArmedFlag" src/discord/nativeProbe.ts && grep -q "1234567890123456789" src/discord/nativeProbe.ts && ! grep -q 'class ' src/discord/nativeProbe.ts && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/discord/nativeProbe.ts` exists and exports `runProbe`, `reconcileArmedFlag`, `testCandidate`, `probeSummary`, and has an `enumerateSurface` producing a `ProbeReport`.
    - The file contains NO `class ` token and NO `for...of` over a Map/Set (`! grep -q 'class ' src/discord/nativeProbe.ts` succeeds); module shape mirrors `random.ts` (`let` + functions).
    - The byte-match reuses `eqBytes` imported from `../selfTest` (no second comparator inlined); the selfTest import is the sanctioned cross-cutting-util sibling import, not an up-graph edge.
    - `testCandidate` sets `settings().nativeProbeArmed` before the call and clears it in a `finally`, uses cheap params `m: 8, t: 1` then real params `m: 65536, t: 3, p: 1, version: 0x13, dkLen: 32`, and byte-matches against `deriveKey` for the 19-byte salt `"1234567890123456789"` via `eqBytes`.
    - The build guard from Plan 01 still passes (`npm run build` exits 0 — nativeProbe.ts does NOT value-import core/stegcloak; it is discord-layer and must not point up-graph).
    - `npm test` exits 0 (byte-compat harness unaffected — COMPAT-01 held).
  </acceptance_criteria>
  <done>nativeProbe.ts enumerates the surface into a persisted ProbeReport, reconciles the armed flag on demand, runs tiered crash-safe candidate tests with the D-09 byte-match (reusing selfTest's eqBytes), and renders a summary — all Hermes-safe and layering-clean; build + harness green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the on-load probe + ProbeReport into src/index.ts (enumeration-only, stale-gated, safe-wrapped)</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (the file being modified — the safe(label,fn) wrapper at 29-38, the init block at 48-70, the __goofcrypt hook at 56-65, onUnload at 81-86)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md § "src/index.ts" (lines 184-213 — the safe('native-probe', maybeRunProbe) wiring, the diag hook ADD, the onUnload symmetry note, bump version)
    - src/discord/nativeProbe.ts (the runProbe / reconcileArmedFlag exports from Task 1)
    - src/settings.ts (settings().nativeProbe and the buildTag staleness check from Plan 01)
  </read_first>
  <action>
    Edit `src/index.ts`. Import `runProbe`, `reconcileArmedFlag` from `./discord/nativeProbe`.
    Add a `maybeRunProbe()` helper: read `settings().nativeProbe`; first call `reconcileArmedFlag(existingReport)` if a report exists (D-05); then run `runProbe()` ONLY when the stored report is missing OR its `buildTag` differs from the currently-detected build tag (D-02 staleness trigger). Enumeration ONLY — it must NEVER invoke native crypto on load (D-03). Wire it inside `onLoad` as `safe("native-probe", maybeRunProbe);` AFTER `initSettings`/`initKeyCache` (so settings() is ready), so a probe failure cannot break plugin init.
    Extend the `__goofcrypt.diag()` hook to surface the persisted report: add `nativeProbe: settings().nativeProbe ?? null` to the returned object (non-secret only — module names + booleans + timing; NO key bytes, NO passwords, per the existing line-54 rule). Bump `version` from 1 to 2 since the diag shape changed.
    Consider onUnload symmetry for the enriched `__goofcrypt` hook (it is left on globalThis today — a pre-existing CONCERNS.md issue; Phase 3 owns the formal fix, but do not worsen it).
    Do NOT auto-invoke any candidate test on load.
  </action>
  <verify>
    <automated>grep -q "runProbe" src/index.ts && grep -q "reconcileArmedFlag" src/index.ts && grep -q "nativeProbe: settings().nativeProbe" src/index.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/index.ts` imports `runProbe` and `reconcileArmedFlag` and wires `safe("native-probe", maybeRunProbe)` inside `onLoad` after settings/keycache init.
    - `maybeRunProbe` runs `runProbe()` only when the stored report is missing or the build tag changed, and calls `reconcileArmedFlag` first; it never invokes native crypto.
    - `__goofcrypt.diag()` returns `nativeProbe` (the persisted report) and `version` is bumped to 2.
    - `npm run build` exits 0 (the on-load wiring survives the Hermes-safe gates and the sync-derive guard).
  </acceptance_criteria>
  <done>On-load enumeration-only probe is wired under safe() with stale-gating and armed-flag reconcile; the persisted ProbeReport surfaces via __goofcrypt.diag(); no native invocation on load; build green.</done>
</task>

<task type="auto">
  <name>Task 3: Add the /encrypt diag --probe / --test verb + ProbeReport render + richer bench to commands.ts</name>
  <files>src/discord/commands.ts</files>
  <read_first>
    - src/discord/commands.ts (the file being modified — the options[] + switch(action) at 30-135, the existing status verb at 118-125, bench at 92-97, the warm() fire-and-forget pattern at 24-28)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md § "src/discord/commands.ts" (lines 152-181 — the diag verb wiring, status render via probeSummary, bench enrichment, the "must not auto-invoke native crypto" rule)
    - src/discord/nativeProbe.ts (runProbe, testCandidate, probeSummary from Task 1 — commands.ts is same-layer, may import directly)
    - src/crypto/argon.ts (the `benchOnceDetailed` + `assertMacrotaskYield` from Plan 02 — bind to the LOCKED return contract `{ totalMs, firstYieldMs, longestBlockMs, yieldCount, ok, form }` specified in 01-02 Task 2; this shape is fixed in the plan, not deferred to the SUMMARY)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-02-SUMMARY.md (confirms benchOnceDetailed landed; the contract itself is locked in the 01-02 plan, not discovered here)
    - src/core/health.ts (healthSummary append pattern — mirror for probeSummary)
  </read_first>
  <action>
    Edit `src/discord/commands.ts`. Import `runProbe`, `testCandidate`, `probeSummary` from `./nativeProbe` and `benchOnceDetailed` + `assertMacrotaskYield` from `../crypto/argon`. Bind to the LOCKED bench contract from 01-02 Task 2: `benchOnceDetailed()` resolves `{ totalMs, firstYieldMs, longestBlockMs, yieldCount, ok, form }` — render those fields directly; do NOT probe the SUMMARY for an unknown shape, the contract is fixed in the plan.
    Add a `diag` action to the existing `switch (action)` (Claude's discretion per D-02/D-05: a `diag` verb with a sub-arg, or distinct verbs — must be discoverable). `diag --probe` (or `action: "probe"`): fire-and-forget `runProbe()` then `showToast` the fresh `probeSummary()` — enumeration only, MUST NOT invoke native crypto. `diag --test` (or `action: "test"`): the ONLY caller of `testCandidate` (D-05) — iterate the reachable candidates from the persisted report, run `testCandidate(name, fn)` for each under the armed-flag protection, and toast per-candidate `{reachable, saltAccepted, outputKind, byteMatch, crashed, timingMs}`. Use the `warm()`/`bench` fire-and-forget `.then/.catch` toast pattern so the UI never blocks. To resolve the candidate `fn` for each name, build the adapter inside nativeProbe (re-resolving the native handle each call — no memoization).
    Extend the `status` verb to append `probeSummary()` to its toast string (after `healthSummary()`), so the persisted report is visible on-device after a restart (SPIKE-01).
    Extend the `bench` verb to surface the richer timing via `benchOnceDetailed()` — render `totalMs`, `firstYieldMs`, `longestBlockMs`, `yieldCount` (zero ⇒ thread-starved) — plus `assertMacrotaskYield()` (the `{ ok, form }` already included in benchOnceDetailed's return) so the on-device caret-regression tripwire is one tap away (D-06b).
    Update the command `description`/`displayDescription` to list the new `diag` action so it is discoverable. Keep the existing set/import/on/off/toggle/cycle behavior intact.
  </action>
  <verify>
    <automated>grep -q "diag" src/discord/commands.ts && grep -q "probeSummary" src/discord/commands.ts && grep -q "testCandidate" src/discord/commands.ts && grep -q "benchOnceDetailed" src/discord/commands.ts && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `src/discord/commands.ts` adds a discoverable `diag` surface with a `--probe` (enumeration-only) path and a `--test` path that is the ONLY caller of `testCandidate`.
    - The `status` verb toast appends `probeSummary()`; the `bench` verb consumes `benchOnceDetailed()`'s locked `{ totalMs, firstYieldMs, longestBlockMs, yieldCount, ok, form }` shape and surfaces the timing + macrotask-form result.
    - `diag --probe` does NOT invoke native crypto (only `runProbe`/enumeration); native invocation happens solely on `diag --test`.
    - `npm run build` exits 0 and `npm test` exits 0 (Hermes-safe gates + byte-compat harness green).
  </acceptance_criteria>
  <done>The /encrypt command exposes a discoverable diag verb (probe = enumerate, test = the sole native-invocation path), status renders the persisted ProbeReport, and bench surfaces the locked enriched timing contract + macrotask tripwire; build + harness green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-device evidence capture — probe persistence, armed-flag crash survival, real-param candidate run (SPIKE-01/02)</name>
  <files>src/discord/nativeProbe.ts, src/discord/commands.ts (on-device verification — no file writes)</files>
  <read_first>
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VALIDATION.md § "Manual-Only Verifications" (the four on-device behaviors + test instructions)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md § "Open Questions" (A1 storage-flush, A5 build-tag reachability)
  </read_first>
  <what-built>
    A permanent on-device native-crypto probe (`nativeProbe.ts`) wired into onLoad (enumeration-only, stale-gated) and the `/encrypt diag --probe`/`--test` + `/encrypt status` + `__goofcrypt.diag()` surfaces. It enumerates the native surface into a persisted ProbeReport, runs crash-safe tiered candidate invocation with armed-flag poison detection, and byte-matches any candidate against the on-device noble reference for a 19-byte salt. The build is Hermes-safe and the byte-compat harness is green.
  </what-built>
  <action>
    CHECKPOINT (human-verify): no code is written in this task — Tasks 1-3 already built and verified the probe + command surface in CI. Build and install the plugin on the PRIMARY device, then gather the on-device evidence per <how-to-verify> (ProbeReport restart-survival, per-candidate salt/output/byteMatch or `none reachable`, armed-flag crash survival + the A1 storage-flush finding, the A5 build-tag finding, and the freeze yield/timing/storm numbers) and paste it back as the resume signal. Plan 04 consumes these values verbatim for the verdict.
  </action>
  <how-to-verify>
    On your PRIMARY device (D-10), with the freshly-built plugin installed:
    1. Restart-survival (SPIKE-01): run `__goofcrypt.diag()` via /eval (or `/encrypt status`) and confirm a `nativeProbe` report renders with enumerated module names, turbo/metro hits, and crypto.subtle. FULLY restart Discord. Run `__goofcrypt.diag()` and `/encrypt status` again — the SAME report must still render (proves persistence, unlike diagnose2.txt). Record the OS + Discord/Hermes build tag shown.
    2. Build-tag staleness (D-02 / A5): note whether a real `buildTag` was captured or it fell back to null (manual-only re-probe). Record which.
    3. Candidate test (SPIKE-02 / D-04 / D-09): run `/encrypt diag --test`. For EACH candidate, record {reachable, salt-accepted (19-byte), output-kind (raw32 / phc-string / other), byteMatch vs noble, timingMs, crashed}. If NO Argon2 candidate is reachable, record `none reachable`.
    4. Armed-flag crash survival (SPIKE-02 / A1 / Pitfall 4): if any candidate hard-crashes the app during `--test`, relaunch and confirm it is marked `crashed/unsafe` and skipped. If none crash, deliberately verify the flush: set the armed flag (trigger a candidate), force-quit Discord mid-call, relaunch, and confirm the flag persisted and the candidate is marked crashed. Record whether the armed write survives a hard crash (the A1 storage-flush finding).
    5. Freeze observation (SPIKE-03, seeds Plan 04): enable the debug flag, open a COLD channel containing several cloaked messages, send a first encrypt, and run `/encrypt bench`. Record: yield-sample count during the derivation (zero ⇒ thread-starved), first-yield ms, longest-block ms, bench total ms, concurrent backgroundDecrypt count (the LOAD_MESSAGES_SUCCESS storm), and the `assertMacrotaskYield()` result (macrotask vs microtask form).
    Paste all recorded numbers/booleans back as the resume signal — Plan 04 consumes them for the verdict.
  </how-to-verify>
  <resume-signal>Paste the recorded ProbeReport summary, per-candidate results, armed-flag/A1 finding, build-tag/A5 finding, and freeze numbers; or describe any issue.</resume-signal>
  <verify>
    <human-check>ProbeReport persists across a full Discord restart; per-candidate salt/output/byteMatch recorded (or `none reachable`); armed-flag crash survival verified; freeze yield/timing/storm numbers captured.</human-check>
  </verify>
  <done>On-device evidence for SPIKE-01 (persisted, restart-survivable report), SPIKE-02 (per-candidate salt/output/byteMatch + armed-flag crash behavior), and SPIKE-03 (yield/timing/storm numbers) is captured and recorded for the Plan 04 verdict.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (nativeProbe + wiring survive the Hermes-safe + sync-derive guards; nativeProbe does not import up-graph).
- `npm test` green (byte-compat harness + Plan 01 assertions) — COMPAT-01 held.
- On-device (manual): ProbeReport persists across a restart; per-candidate results recorded; armed-flag crash survival confirmed; freeze numbers captured.
</verification>

<success_criteria>
- A persisted, restart-survivable ProbeReport enumerates the reachable native surface and renders via __goofcrypt.diag() + /encrypt status (SPIKE-01).
- Every reachable Argon2 candidate has a recorded on-device result: salt-accepted (19-byte) + raw-32-byte vs PHC + byteMatch vs noble — or `none reachable` (SPIKE-02 / D-04 / D-09).
- Native invocation is crash-safe (armed-flag poison detection, manual-trigger-only) and the A1 storage-flush finding is documented (SPIKE-02 / D-05).
- The real freeze path is observed on-device (yield count, first-yield, storm concurrency) — feeding the Plan 04 verdict (SPIKE-03).
</success_criteria>

<output>
Create `.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-03-SUMMARY.md` when done. Record: the chosen ProbeReport field layout as built, the diag command surface chosen, the on-device evidence (per-candidate results / `none reachable`, restart-survival result, armed-flag A1 finding, build-tag A5 finding, freeze yield/timing/storm numbers) — Plan 04 consumes these verbatim for the verdict.
</output>
