---
phase: 01-spike-capture-the-probe-diagnose-the-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.ts
  - tests/harness.ts
  - scripts/build.mjs
autonomous: true
requirements: [SPIKE-01, SPIKE-03]
must_haves:
  truths:
    - "The ProbeReport schema round-trips through JSON.stringify/parse with deep equality (CI-tested, off-device)"
    - "npm test asserts deriveKey('goofcryptspikevector','1234567890123456789') equals a committed 32-byte D-09 reference vector cross-checked against stegcloak-rs"
    - "npm test asserts the patched noble nextTick is the macrotask (setTimeout) form, not the empty-async-arrow microtask form"
    - "npm run build fails when any src/discord/* module transitively value-imports src/core/stegcloak.ts; exits 0 on the clean current tree"
  artifacts:
    - path: "src/settings.ts"
      provides: "ProbeReport / CandidateResult interfaces + nativeProbe, nativeProbeArmed, debugInstrument fields in Settings + DEFAULTS"
      contains: "interface ProbeReport"
    - path: "tests/harness.ts"
      provides: "Wave-0 CI assertions: ProbeReport round-trip [7], nextTick macrotask form [8], D-09 reference vector [9]"
      contains: "1234567890123456789"
    - path: "scripts/build.mjs"
      provides: "metafile:true + sync-derive import-graph build guard (D-07)"
      contains: "reachesSyncDerive"
  key_links:
    - from: "scripts/build.mjs"
      to: "src/core/stegcloak.ts"
      via: "esbuild metafile import-graph walk from src/discord/* entries"
      pattern: "stegcloak"
    - from: "tests/harness.ts"
      to: "src/crypto/argon.ts deriveKey"
      via: "D-09 reference-key vector assertion"
      pattern: "deriveKey"
---

<objective>
Lay the off-device CI foundation for the whole spike: the persisted `ProbeReport` schema (Claude's-discretion field layout per D-01), the three Wave-0 harness assertions that make the spike's evidence CI-checkable, and the permanent `scripts/build.mjs` sync-derive guard (D-07) that records the completed import-graph audit as a structural invariant.

Purpose: Every downstream plan consumes contracts defined here — the `ProbeReport`/`CandidateResult` types, the `nativeProbe`/`nativeProbeArmed`/`debugInstrument` settings fields, and the D-09 reference-key vector. Defining them first prevents the executor scavenger-hunt and lets Plans 02/03/04 build against fixed shapes. The build guard makes the "no sync-derive leak" finding (SPIKE-03's hardest sub-question, already answered by research) un-regressable.

Output: Updated `src/settings.ts`, `tests/harness.ts`, `scripts/build.mjs`. `npm test` green, `npm run build` exits 0 on the clean tree and throws on a deliberately-added discord→stegcloak value import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md
@.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the ProbeReport schema + spike settings fields in settings.ts</name>
  <files>src/settings.ts</files>
  <read_first>
    - src/settings.ts (the file being modified — Settings interface + DEFAULTS + initSettings back-fill pattern)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md § "Shared Patterns › Persistence via settings() + DEFAULTS" (lines 290-313 — the exact ADD points)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md § "Runtime State Inventory" (NEW stored data: nativeProbe + nativeProbeArmed + debugInstrument; no secret material rule)
    - src/core/keycache.ts (KeyCacheStore — the precedent for extending Settings with a persisted sub-store)
  </read_first>
  <behavior>
    - A ProbeReport built with all fields populated, then JSON.stringify'd and JSON.parse'd, deep-equals the original (no Map/Set/typed-array fields that break serialization — plain objects, arrays, strings, numbers, booleans only).
    - DEFAULTS.nativeProbe is null, DEFAULTS.nativeProbeArmed is null, DEFAULTS.debugInstrument is false.
    - initSettings back-fills nativeProbe/nativeProbeArmed/debugInstrument on an existing install that lacks them (the existing Object.keys(DEFAULTS) loop already does this — verify it covers the new keys).
  </behavior>
  <action>
    Add to `src/settings.ts` exported interfaces describing the persisted, restart-survivable ProbeReport (Claude's discretion on exact layout per D-01 — keep it plain-JSON-serializable, NO Uint8Array/Map/Set fields, store only non-secret data: module names, booleans, counts, timing, build tag).
    Define `interface ProbeReport` with at minimum: `version: number`, `timestamp: number`, `buildTag: string | null` (the Discord/Hermes build tag for the D-02 staleness check + D-10 device-coverage record), `platform: string | null` (OS, D-10), `scannedKeys: number`, `cryptoIsh: string[]` (nativeModuleProxy regex matches), `turboHits: Array<{ name: string; methods: string[] }>`, `metroHits: Array<{ prop: string; found: boolean; methods: string[] }>`, `subtle: boolean`, `candidates: CandidateResult[]`, `verdict: "GREEN" | "RED" | "untested"`.
    Define `interface CandidateResult` with: `name: string`, `reachable: boolean`, `saltAccepted: boolean`, `outputKind: "raw32" | "phc-string" | "other" | "unknown"`, `byteMatch: boolean`, `crashed: boolean`, `error?: string`, `timingMs?: number`.
    Extend the `Settings` interface with three optional fields: `nativeProbe?: ProbeReport | null`, `nativeProbeArmed?: string | null`, `debugInstrument?: boolean`.
    Add the corresponding entries to `DEFAULTS`: `nativeProbe: null, nativeProbeArmed: null, debugInstrument: false`.
    Do NOT touch any existing field. The existing `initSettings` back-fill loop over `Object.keys(DEFAULTS)` already covers the new keys — confirm, do not duplicate. No `class` syntax; this is interfaces + a plain object literal only.
  </action>
  <verify>
    <automated>grep -q "interface ProbeReport" src/settings.ts && grep -q "interface CandidateResult" src/settings.ts && grep -q "debugInstrument: false" src/settings.ts && ! grep -q 'class ' src/settings.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/settings.ts` contains `interface ProbeReport` and `interface CandidateResult`, both exported.
    - `src/settings.ts` DEFAULTS object literal contains `nativeProbe: null`, `nativeProbeArmed: null`, and `debugInstrument: false`.
    - `npm run build` exits 0 (the new code survives the swc ES5 + class/generator/iterator gates).
    - No `class ` token introduced in `src/settings.ts` (`! grep -q 'class ' src/settings.ts` succeeds).
  </acceptance_criteria>
  <done>ProbeReport + CandidateResult interfaces exist and are exported; the three spike settings fields default cleanly through DEFAULTS; build passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the three Wave-0 CI assertions to tests/harness.ts</name>
  <files>tests/harness.ts</files>
  <read_first>
    - tests/harness.ts (the file being modified — the check(name,cond,detail) helper at lines 25-33, CHANNEL="1234567890123456789" at line 35, the numbered-section convention [1]..[6])
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md § "tests/harness.ts" (lines 271-287 — the three Wave-0 assertions, exact)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-VALIDATION.md § "Wave 0 Requirements" (lines 67-73) and § "Per-Task Verification Map" (✅ W0 rows)
    - src/crypto/argon.ts (the sync deriveKey at line 27 — the D-09 reference key source; OPTS params m=65536,t=3,p=1,version:0x13,dkLen:32)
    - src/settings.ts (the ProbeReport interface from Task 1 — for the round-trip assertion's sample object)
    - scripts/build.mjs (lines 29-42 — the noble-macrotask-yield esbuild plugin that rewrites nextTick; needed to understand whether the test bundle applies the patch)
  </read_first>
  <behavior>
    - Section [7] ProbeReport round-trip: a fully-populated ProbeReport literal → JSON.stringify → JSON.parse deep-equals the original; check() passes.
    - Section [8] nextTick macrotask form: importing the runtime nextTick and asserting String(nextTick) does NOT match the empty-async-arrow microtask form (`/async\s*\(\s*\)\s*=>\s*\{\s*\}/`) passes when the macrotask patch is in effect; if the test bundle does not apply the build-time patch, the assertion targets the bundled symbol's form and is documented accordingly (see PATTERNS note).
    - Section [9] D-09 reference vector: deriveKey("goofcryptspikevector","1234567890123456789") equals a committed 32-byte expected array, AND that same 32 bytes is cross-checked against stegcloak-rs (sc.hide/reveal round-trip already proves the noble path is byte-compat; assert the raw key bytes against a committed literal).
  </behavior>
  <action>
    Append three new numbered sections to `tests/harness.ts` using the existing `check(name, cond, detail)` helper — do NOT introduce a new test framework.
    Section `[7] ProbeReport serialization round-trip`: construct a representative ProbeReport object literal (matching the Task-1 interface — buildTag, candidates[] with one CandidateResult, etc.), `JSON.stringify` then `JSON.parse`, and `check` deep-equality (compare via `JSON.stringify(a) === JSON.stringify(b)` after a stable re-stringify, or a small recursive eqDeep — Node side, `for...of` is fine here). This proves SPIKE-01 persistability off-device.
    Section `[8] nextTick macrotask-form assertion`: `import { nextTick } from "@noble/hashes/utils"`, compute `const src = String(nextTick)`, and `check("nextTick is macrotask form", !/async\s*\(\s*\)\s*=>\s*\{\s*\}/.test(src), src.slice(0,80))`. This is the CI counterpart to the on-device D-06 tripwire. If the harness bundle does NOT receive the build-time patch (the patch lives in scripts/build.mjs, not scripts/test.mjs), document that with a one-line comment and still assert the form — coordinate the exact expectation with how scripts/test.mjs bundles; the on-device assertMacrotaskYield() (Plan 02) is the runtime counterpart.
    Section `[9] D-09 reference-key vector`: `import { deriveKey } from "../src/crypto/argon"`. Define `const VEC_PW = "goofcryptspikevector"` and reuse the existing `CHANNEL = "1234567890123456789"` (19-byte snowflake salt). Compute `const key = deriveKey(VEC_PW, CHANNEL)`. Commit the resulting 32 bytes as an expected `Uint8Array`/number[] literal in the test (the executor runs deriveKey once, captures the bytes, and pastes them as the committed vector). `check` that the live derivation `eqBytes` the committed literal. Additionally cross-check the noble path is GoofCord-compatible by `check`-ing that `ourReveal(sc.hide("x", VEC_PW, CHANNEL, "cover"), VEC_PW, CHANNEL) === "x"` (reuses the existing stegcloak-rs `sc` instance) so the committed vector is provably from the byte-compat path, not a typo. This is the on-device byte-match's CI-proven target (D-09). Note the Phase-2 GATE-01 overlap in a comment: Phase 1 commits only the reference VALUE, not the structural gate.
    Keep the closing `passed/failed` summary and `process.exit` logic intact (it already counts all sections).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `tests/harness.ts` contains a `[7]` ProbeReport round-trip section, a `[8]` nextTick macrotask-form section, and a `[9]` D-09 reference-vector section.
    - `tests/harness.ts` contains the literal `goofcryptspikevector` and a committed 32-byte expected vector.
    - `npm test` exits 0 with all sections passing (the closing line reads `✅ harness: N passed, 0 failed`).
    - The D-09 section both asserts `deriveKey(VEC_PW, CHANNEL)` against the committed literal AND round-trips through the stegcloak-rs `sc` reference (byte-compat proof, not a trusted typo).
  </acceptance_criteria>
  <done>All three Wave-0 assertions are present and green under `npm test`; the D-09 reference vector is committed and cross-checked against stegcloak-rs.</done>
</task>

<task type="auto">
  <name>Task 3: Add the sync-derive import-graph build guard to scripts/build.mjs (D-07)</name>
  <files>scripts/build.mjs</files>
  <read_first>
    - scripts/build.mjs (the file being modified — the esbuild build({...}) call at lines 45-60 currently lacks metafile:true; the class/generator/iterator regex gates at lines 81-91 are the throw-on-violation template)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-PATTERNS.md § "scripts/build.mjs" (lines 230-267 — the exact reachesSyncDerive walk + metafile note + A2 type-erasure caveat)
    - .planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-RESEARCH.md § "Code Examples › Sketch: the sync-derive build guard" (lines 436-466) and § "Completed Audit" (the clean-tree invariant the guard protects)
    - src/core/stegcloak.ts (the sole sync-deriveKey consumer the guard targets) and src/core/encrypt.ts (the `import type { RandomBytes }` type-only import that must NOT false-trip)
  </read_first>
  <action>
    Add `metafile: true` to the existing esbuild `build({...})` options object in `scripts/build.mjs` (it currently does not pass it). Capture `const meta = result.metafile`.
    After the bundle is produced but BEFORE the swc transform (i.e. between line 61 `const bundled = ...` and line 64 `const { code: lowered } = await transform(...)`), add a static import-graph guard mirroring the existing regex-gate throw style:
    Define `function reachesSyncDerive(entry, seen = new Set())` that returns true if `entry` matches `/src[\\/]core[\\/]stegcloak\.ts$/`, else recurses over `meta.inputs[entry]?.imports ?? []` (use a `for (let i = 0; ...)` index loop — this is Node so for...of is also fine, but match the Hermes-safe convention) calling `reachesSyncDerive(imports[i].path, seen)` with cycle protection via `seen`.
    Compute `const discordEntries = Object.keys(meta.inputs).filter((p) => /src[\\/]discord[\\/]/.test(p))` and loop: if any `reachesSyncDerive(discordEntries[i])` is true, `throw new Error("sync-derive leak: " + discordEntries[i] + " transitively imports core/stegcloak.ts (sync 64MiB derive) — would re-freeze the UI")`.
    Rely on esbuild erasing `import type {...}` before the metafile so `core/encrypt.ts`'s `import type { RandomBytes } from "./stegcloak"` does NOT appear as a value import (A2). Verify this assumption holds: run `npm run build` (must exit 0 on the clean tree). Then prove the guard fires by temporarily adding a real value import of `core/stegcloak` into a `src/discord/*` file, confirm `npm run build` throws the sync-derive-leak error, then REVERT that temporary edit. Document the before/after result in the SUMMARY (this is the recorded build-guard validation, complementing the already-completed grep audit recorded in Plan 04's verdict).
    Do not change the emitted bundle shape or the manifest — the metafile is build-time-only.
  </action>
  <verify>
    <automated>grep -q "metafile: true" scripts/build.mjs && grep -q "reachesSyncDerive" scripts/build.mjs && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/build.mjs` passes `metafile: true` to the esbuild `build()` call and defines `reachesSyncDerive`.
    - `npm run build` exits 0 on the clean current tree (no discord→stegcloak value import exists — the research audit confirms this).
    - When a value import of `../core/stegcloak` is temporarily added to any `src/discord/*` file, `npm run build` throws an error whose message contains `sync-derive leak` (demonstrated then reverted — recorded in SUMMARY).
    - The guard runs after the esbuild bundle and before the swc transform.
  </acceptance_criteria>
  <done>The build guard is wired with `metafile: true`; clean tree builds; a deliberately-injected discord→stegcloak value import fails the build with the sync-derive-leak message; the temporary injection is reverted.</done>
</task>

</tasks>

<verification>
- `npm test` green (existing [1]-[6] byte-compat sections + new [7]-[9] Wave-0 assertions) — COMPAT-01 hard gate held.
- `npm run build` exits 0 on the clean tree; the sync-derive guard demonstrably throws on a discord→stegcloak value import.
- `src/settings.ts` exports `ProbeReport` + `CandidateResult` and defaults the three spike fields.
- No `class`/generator/iterator syntax introduced (build gates pass).
</verification>

<success_criteria>
- The ProbeReport persistence schema is defined and round-trips through JSON in CI (SPIKE-01 off-device half).
- The D-09 reference-key vector is committed and cross-checked against stegcloak-rs (SPIKE-04 honest-verdict CI target).
- The nextTick macrotask-form assertion runs in CI (SPIKE-03 caret-regression tripwire, off-device half).
- The permanent sync-derive build guard records the completed import-graph audit as an un-regressable structural invariant (SPIKE-03 / D-07).
</success_criteria>

<output>
Create `.planning/phases/01-spike-capture-the-probe-diagnose-the-freeze/01-01-SUMMARY.md` when done. Record: the exact ProbeReport field layout chosen, the committed D-09 reference vector bytes (first/last few + length), the nextTick-in-test-bundle finding (does scripts/test.mjs apply the patch?), and the build-guard fire/revert demonstration result.
</output>
