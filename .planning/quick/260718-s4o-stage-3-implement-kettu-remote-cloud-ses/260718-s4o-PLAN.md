---
quick_id: 260718-s4o
phase: quick
plan: 260718-s4o
type: execute
wave: 1
depends_on: [260718-qlx]
status: planned
description: Stage 3 - implement Kettu remote cloud session, strict API client, revision-aware ordered cache, and explicit UX
autonomous: true
requirements: [REMOTE-KDF-STAGE-3]
files_modified:
  - src/cloud/client.ts
  - src/cloud/session.ts
  - src/cloud/remoteKdf.ts
  - src/core/remoteKeycache.ts
  - src/settings.ts
  - src/index.ts
  - src/ui/Settings.tsx
  - src/discord/commands.ts
  - tests/harness.ts
  - tests/remoteKdfStage3.ts
  - docs/REMOTE_KDF_MOBILE_TRANSPORT.md
  - README.md
must_haves:
  truths:
    - "Manual/local password mode remains the untouched default and its passwords, passwordId-indexed settings.keys cache, imports, chosen slot, and commands remain distinct from remote state; Stage 3 adds no implicit manual-to-remote or remote-to-manual fallback and does not modify Flux/send cold paths"
    - "Remote configuration accepts only a normalized HTTPS origin, except exact localhost/127.0.0.1/[::1] HTTP when a separate explicit development flag is enabled, and imports only the existing raw 32-character lowercase cloud token without OAuth redesign, Bearer wrapping, credentials, path/query/fragment, redirect, or /v1/save access"
    - "cloudEncryptionKey lives only in a module-level session holder, is non-empty and bounded to the frozen 1024 UTF-8 bytes, and every accepted host/token/localhost-policy change, forget action, replacement, or plugin unload aborts requests and calls clearRemoteSession so the old key can never reach a new origin; the key is never written to plugin storage, logs, toasts, status, diagnostics, command replies, cache/export shapes, or caught error text"
    - "Before networking, a fail-closed runtime probe proves fetch, AbortController, URL parsing, required Response/header methods, and a supported response-bounding mode; missing or partial Hermes/Kettu globals yield stable REMOTE_UNSUPPORTED without crashing load or falling back permissively, while real-device redirect/abort confirmation is explicitly recorded for Stage 5"
    - "The mobile client calls only POST /v2/kdf/derive and GET /v2/kdf/revision with the exact Stage 1 schemas and raw Authorization header, uses fixed bounded abort timeouts including a 270000 ms derive batch budget, hard-caps responses at 16384 bytes through streaming or a required bounded Content-Length fallback, rejects redirects/status-shape mismatches/extras/noncanonical keys, and exposes only stable code-only server or client errors without response/request/exception reflection"
    - "A versioned remoteKeyCache is migrated beside, never over, the existing manual keys store; every persisted and loaded remote key is canonical padded base64 decoding to exactly 32 bytes, entries contain only revision, ordered key slots, and sendCapable, and no password, passwordId, cloud key, token, account identity, or mutable KDF option is stored"
    - "For each channel, a successful derive installs its exact contiguous slot order as the sole send-capable current revision, demotes every older revision to decrypt-only, de-duplicates the same revision, and retains at most three sets total (current plus two old); a shared ordered cache-mutation token prevents late derive/revision/derive responses from resurrecting stale state, and a global revision change immediately demotes all cached channels so stale keys can never remain send-capable"
    - "Revision checks run once on configured plugin load, always on explicit refresh, and through a fixed five-minute coalescing TTL API reserved for Stage 4 conservative pre-send use; revision GET never uses the cloud key or marks readiness, failures do not advance freshness, and no per-message polling is introduced"
    - "Remote session readiness becomes true only after valid host/token plus the in-memory cloud key produce a strict non-empty derive response proving the server decrypted a password-bearing blob; revision-only success, cached keys, configuration, token possession, malformed responses, and failed derives never mark ready, while key/config/revision change or unload clears verification"
    - "Settings and /encrypt collectively expose explicit safe setup, verify/refresh-current-channel, check-revision, status, clear-remote-cache, clear-session-key, and forget-token controls, while secret-bearing token/key setters exist only in the masked settings UI; inputs are cleared locally, status contains only booleans/counts/timestamps/stable codes, and remote clear operations preserve the manual cache and settings"
    - "Plugin lifecycle initializes the remote migration/coordinator only after settings, performs best-effort non-blocking on-load revision refresh, aborts in-flight remote requests and clears all transient secret/readiness state on unload, preserves Hermes build constraints and byte compatibility, and leaves src/discord/flux.ts and src/discord/send.ts for Stage 4"
  artifacts:
    - path: "src/cloud/client.ts"
      provides: "Fail-closed runtime capability probe plus strict no-redirect v2 derive/revision fetch boundary with bounded timeouts, hard response limits, raw-token auth, and stable errors"
      contains: "/v2/kdf/derive"
    - path: "src/cloud/session.ts"
      provides: "Memory-only cloud-key and verified-session holder with secret-safe status and unload clearing"
      contains: "clearRemoteSession"
    - path: "src/core/remoteKeycache.ts"
      provides: "Versioned ordered revision cache, manual-preserving migration, canonical key gates, demotion, retention, and clear APIs"
      contains: "MAX_REMOTE_REVISIONS_PER_CHANNEL"
    - path: "src/cloud/remoteKdf.ts"
      provides: "Derive/revision orchestration, readiness transitions, load/explicit/TTL refresh, request coalescing, and shutdown"
      contains: "REMOTE_REVISION_TTL_MS"
    - path: "src/settings.ts"
      provides: "Persisted non-secret remote origin/development flag, revocable raw token, and separate remote cache envelope"
      contains: "remoteKeyCache"
    - path: "src/ui/Settings.tsx"
      provides: "Masked token/session-key setup plus verify, revision, status, clear, and forget controls separate from manual settings"
      contains: "Remote KDF"
    - path: "src/discord/commands.ts"
      provides: "Copyable but non-secret remote status/refresh/check/clear command actions without secret arguments"
      contains: "remote-status"
    - path: "src/index.ts"
      provides: "Remote init/load refresh and unload abort/session clearing wired after settings without changing message hooks"
      contains: "shutdownRemoteKdf"
    - path: "tests/remoteKdfStage3.ts"
      provides: "Focused deterministic client, session, migration, cache, revision TTL, readiness, lifecycle, and secret-safety coverage"
      contains: "CLOUD_DECRYPT_FAILED"
    - path: "docs/REMOTE_KDF_MOBILE_TRANSPORT.md"
      provides: "Stage 3 runtime capability contract and explicit pending Stage 5 real-device redirect/abort verification checklist"
      contains: "Stage 5"
  key_links:
    - from: "src/settings.ts"
      to: "src/core/remoteKeycache.ts"
      via: "Settings extends the versioned remote cache store under remoteKeyCache while retaining the independent existing keys field"
      pattern: "RemoteKeyCacheStore|remoteKeyCache"
    - from: "src/cloud/client.ts"
      to: "src/cloud/contracts.ts"
      via: "Every success/error body crosses the frozen exact response parsers before it can reach session or storage"
      pattern: "parseDeriveResponse|parseRevisionResponse|parseErrorResponse"
    - from: "src/cloud/remoteKdf.ts"
      to: "src/cloud/session.ts"
      via: "The coordinator reads the cloud key only for derive, marks verified only after strict derive success, and clears verification on stale/config/shutdown transitions"
      pattern: "remoteCloudKey|markRemoteVerified|clearRemoteSession"
    - from: "src/cloud/remoteKdf.ts"
      to: "src/core/remoteKeycache.ts"
      via: "Strict derive responses are installed in ordered current/old storage and revision responses globally demote stale send entries"
      pattern: "storeRemoteDerivedKeys|applyRemoteRevision"
    - from: "src/index.ts"
      to: "src/cloud/remoteKdf.ts"
      via: "Lifecycle initializes after settings/cache, starts one caught load refresh, then aborts requests and clears the memory-only session on unload"
      pattern: "initRemoteKdf|refreshRemoteRevisionOnLoad|shutdownRemoteKdf"
    - from: "src/ui/Settings.tsx"
      to: "src/cloud/remoteKdf.ts"
      via: "UI imports host/token, sets the session-only key, verifies the current channel, checks revision, reads safe status, and clears remote-only state"
      pattern: "setRemoteCloudKey|refreshRemoteChannel|remoteKdfStatus|clearRemote"
    - from: "src/discord/commands.ts"
      to: "src/cloud/remoteKdf.ts"
      via: "Remote actions call only secret-free status/refresh/check/clear functions; token and cloud key are never command options or replies"
      pattern: "remote-status|remote-refresh|remote-check|remote-clear"
---

<objective>
Implement Stage 3 of `docs/REMOTE_KDF_ARCHITECTURE.md` in the Kettu plugin: safely import the existing cloud endpoint/token, hold the cloud encryption key for only the current plugin session, consume the frozen Stage 2 v2 API through a strict bounded client, persist ordered revision-aware channel keys in a separate versioned cache, and expose explicit setup/refresh/status/clear controls.

Purpose: Establish the complete mobile trust, storage, and API boundary needed to replace slow local Argon on cold channels without storing a universal secret, poisoning the cache with untrusted JSON, overwriting GoofCord settings, or blurring remote mode with the existing manual fallback.

Output: Tested cloud client/session/coordinator modules, a migrated bounded remote cache, lifecycle integration, and secret-safe UI/command controls. Stage 4 alone will connect these APIs to inbound Flux and outbound send cold paths.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@docs/REMOTE_KDF_ARCHITECTURE.md
@.planning/quick/260718-prx-stage-1-freeze-remote-kdf-contracts-exac/260718-prx-VERIFICATION.md
@.planning/quick/260718-qlx-stage-2-implement-authenticated-remote-k/260718-qlx-PLAN.md
@.planning/quick/260718-qlx-stage-2-implement-authenticated-remote-k/260718-qlx-VERIFICATION.md
@src/cloud/contracts.ts
@src/settings.ts
@src/core/keycache.ts
@src/ui/Settings.tsx
@src/discord/commands.ts
@src/index.ts
@tests/harness.ts
@scripts/build.mjs
@../goofcord-cloudserver/src/kdf/contracts.ts
@../goofcord-cloudserver/src/routes/v2.ts
</context>

<constraints>
- Treat every locked decision, mobile storage/session requirement, Stage 3 item, acceptance criterion, and secret boundary in `docs/REMOTE_KDF_ARCHITECTURE.md` as canonical. Byte-exact GoofCord compatibility and the frozen Stage 1 contract win over convenience.
- Make changes only in `kettu-goofcrypt`. Do not modify `goofcord-cloudserver`, `stegcloak-rs`, or the nested GoofCord checkout. Preserve user-owned `CLAUDE.md` and `AGENTS.md` changes.
- Reuse `src/cloud/contracts.ts` without weakening its exact-key/exact-shape rules or changing any frozen server code/status mapping. Requests may contain only version, decimal channelId, and cloudEncryptionKey; never add account identity, password list, passwordId, salt, KDF options, output length, or concurrency knobs.
- Keep remote and manual sources structurally separate. Existing `Settings.keys`, passwords, `passwordId()`, local/imported cache, chosen password, and `/encrypt on/off/cycle/import` behavior remain unchanged. Do not route remote failures into local derivation and do not expose a remote-mode switch that the still-manual message hooks could silently ignore.
- Do not edit `src/discord/flux.ts`, `src/discord/send.ts`, `src/core/decrypt.ts`, or `src/core/encrypt.ts` in Stage 3. The one-pending-operation-per-channel, structural payload gate, retry/redispatch, reject-retain-resend, failure cooldown, and no-plaintext-downgrade wiring belong to Stage 4.
- Persist only a normalized remote origin, explicit localhost-development flag, revocable existing raw cloud token, revision metadata, and channel-scoped keys in Kettu plugin storage. Never persist the cloud encryption key, plaintext cloud settings/passwords, remote response bodies, request bodies, account IDs, or derived-key password identifiers.
- The token is the existing 32-character lowercase hexadecimal raw Authorization value and is sent as-is, not as `Bearer`. It may be stored only under the documented casual-privacy limitation and must never enter logs, debug hooks, status/replies, error text, or key exports.
- Accept only an absolute HTTPS origin with no credentials/path/query/fragment. Permit HTTP only for exact `localhost`, `127.0.0.1`, or `[::1]` with the separate explicit development flag. Reject redirects so Authorization/cloud-key material cannot be forwarded or downgraded.
- Treat accepted configuration changes as a security boundary: after validating the complete replacement, abort every request tied to the previous generation and call `clearRemoteSession()` before the replacement can be used. Forgetting credentials follows the same path. A managed key from one origin/token generation must never be reused against another.
- Do not infer mobile networking support from DOM types, Node tests, or a successful eval build. Probe required globals and response capabilities at runtime, fail closed as `REMOTE_UNSUPPORTED` when any required behavior is unavailable, and add no XMLHttpRequest/permissive redirect fallback in Stage 3. Keep real-device redirect-error and abort semantics as an explicit Stage 5 gate.
- Bound response bytes before JSON parsing: reject an oversized declared length before reading; otherwise prefer incremental `Response.body.getReader()` consumption and abort above 16 KiB. If streaming is absent, read text only with a valid bounded `Content-Length`; a missing/malformed length is `REMOTE_UNSUPPORTED` and must not be read.
- Use no new dependency and preserve Hermes/Kettu constraints: no runtime `Buffer` or `TextEncoder`, no WASM/native module, no generator-dependent built syntax, no unbounded iterator lowering, and no `class` syntax in `site/index.js`. Use the existing UTF-8/base64 helpers and index loops where appropriate.
- All network/config/protocol failures must collapse to bounded stable codes. Do not store or serialize a caught message, raw URL input, response body, token, cloud key, channel ID, revision value, or returned key in errors, health, telemetry, debug output, toasts, or command replies.
- Never call `/v1/save`, `/v1/load`, or `/v1/delete`. Stage 3 treats the existing GoofCord blob as server-owned read-only input reachable only through `/v2/kdf/derive` and `/v2/kdf/revision`.
- All derive and revision cache mutations share a monotonic request-order token and mutation epoch. A derive captures configuration generation, mutation epoch, and starting authoritative revision and may install keys or verify readiness only if all three still match at commit; older operations cannot overwrite newer applied state.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the strict remote configuration, memory-only session, and v2 client boundary</name>
  <files>src/cloud/client.ts, src/cloud/session.ts, src/settings.ts, tests/remoteKdfStage3.ts, tests/harness.ts</files>
  <behavior>
    - `probeRemoteTransport()` is exception-safe and proves the actual runtime has callable `fetch`, constructible/abortable `AbortController`, standards-compatible `URL` parsing for the allowed origins, response status/headers/text methods, and either an incremental `body.getReader()` path or the bounded-Content-Length fallback. Missing/partial globals or methods return only `REMOTE_UNSUPPORTED`, perform no request/read, do not crash plugin load, and never select a permissive fallback.
    - Remote origin validation accepts canonical `https://host[:port]` origins only. It rejects credentials, non-root paths, query/fragment, opaque/malformed URLs, non-HTTPS schemes, and lookalike localhost names. `http://localhost[:port]`, `http://127.0.0.1[:port]`, and `http://[::1][:port]` pass only when `remoteAllowInsecureLocalhost` is explicitly true; the normalized persisted value is `URL.origin` with no trailing path.
    - Existing cloud token import trims paste-only surrounding whitespace, then requires exactly `/^[a-f0-9]{32}$/`; production requests send that raw value in `Authorization`, never prepend `Bearer`, never place it in a URL/body, and never echo it. After an entire replacement validates, every actual origin/token/localhost-policy change and every forget action aborts old-generation requests and calls `clearRemoteSession()` before the replacement is usable, clearing both cloud-key and verification references.
    - The session holder accepts only a non-empty cloud key within the frozen 1,024 UTF-8-byte limit, retains the managed string in module memory only, exposes only a callback/internal accessor needed by derive, and offers no serialization/export/debug getter. Replacement and `clearRemoteSession()` drop all key and verification references; managed-string erasure limits are documented honestly.
    - `derive(channelId, cloudKey)` sends exact JSON `{version:1,channelId,cloudEncryptionKey}` to `<origin>/v2/kdf/derive`; `revision()` sends a bodyless GET to `<origin>/v2/kdf/revision`. Both use `redirect:"error"`, `cache:"no-store"`, raw Authorization, exact methods, and no query. The client has injectable fetch/timer hooks for tests but production uses global fetch/AbortController.
    - Freeze non-user-adjustable `REMOTE_DERIVE_TIMEOUT_MS=270000`, explicitly eight Stage 2 slots times the default 30000 ms worker-job budget plus 30000 ms cloud-scrypt/network overhead; also freeze `REMOTE_REVISION_TIMEOUT_MS=15000` and `MAX_REMOTE_RESPONSE_UTF8_BYTES=16384`. One AbortController covers fetch and body consumption and is aborted on timeout/shutdown.
    - Enforce the 16 KiB response cap before JSON parsing. Canonical declared lengths above the cap fail before reading. When `body.getReader()` exists, consume Uint8Array chunks incrementally, track total bytes, cancel/abort immediately above the cap, and concatenate only bounded data. Without streaming, require a canonical bounded `Content-Length` before calling `text()`, then verify actual UTF-8 bytes remain within the cap; missing/malformed length or missing required response methods fails `REMOTE_UNSUPPORTED` without reading.
    - Require JSON and accept success only at HTTP 200 through the corresponding Stage 1 exact parser; accept server errors only when `parseErrorResponse` succeeds and `KDF_ERROR_STATUS[code]` exactly matches the HTTP status. Freeze code-only client failures including `REMOTE_NOT_CONFIGURED`, `REMOTE_KEY_REQUIRED`, `REMOTE_TIMEOUT`, `REMOTE_UNAVAILABLE`, `REMOTE_PROTOCOL_ERROR`, and `REMOTE_UNSUPPORTED` alongside the seven server codes. Redirects, malformed JSON, success/error extras, mismatched status/code, missing/noncanonical/31-byte keys, partial APIs, and caught network/parser detail expose only a stable code without reflection.
  </behavior>
  <action>
    Add the focused Stage 3 test module first and import/await it from the existing harness before the final pass/fail summary. Reuse the harness's simple `check` style rather than introducing Jest/Vitest. Give tests injected fetch, AbortController, clock/timer, and storage seams so they deterministically capture requests, drive aborts, and inspect state without real network access.

    Extend `Settings` only with primitive remote configuration fields (`remoteHost`, `remoteAuthToken`, `remoteAllowInsecureLocalhost`) using non-null defaults compatible with Kettu's null-hostile reactive proxy. Do not add a cloud-key field or default. Put normalization/token validation behind named setters so UI code never directly persists unchecked values. A config update must be all-or-nothing: validate host, policy, and token first; if the normalized tuple changes, abort old requests and call `clearRemoteSession()` before assigning/incrementing the configuration generation. An invalid or identical update leaves state untouched. Forgetting credentials uses the same abort/clear/generation path before primitive storage fields are cleared.

    Implement `session.ts` as closure/module state rather than plugin storage. Export setters/clearers and a redacted state function returning booleans only. Keep verification state separate from mere key presence and make the only marking function internal/narrow enough for Task 2's coordinator. Never include the key in thrown errors, toast strings, `JSON.stringify`-able status, or callbacks beyond the immediate derive call.

    Implement `client.ts` as a strict functional boundary around the already-frozen mobile contracts. Probe the actual globals without throwing before constructing a request; validate configuration; join only the two literal v2 paths to the normalized origin; use exact raw Authorization; install an AbortController per request; track controllers so configuration changes/unload can abort all; and clear timers/controller tracking in `finally`. Check redirect flags/final URL as defense in depth even with `redirect:"error"`. Implement a bounded body reader with the exact streaming/declared-length decision above, using the existing Uint8Array UTF-8 helper rather than Buffer/TextEncoder. Map all caught values without retaining their messages or bodies.

    Tests must cover every host/token edge, raw header and exact URL/body/method, no `/v1` request, no redirect follow, the exact 270000/15000 timeout constants and derive-budget rationale, timeout/shutdown abort during fetch and streaming read, all seven server code/status pairs, status mismatches, every malformed success/error shape, and exact multi-slot success. Test complete, missing, and partial `fetch`/`AbortController`/`URL`/Response APIs; missing globals must not crash load or call fetch and must report `REMOTE_UNSUPPORTED`. Test declared oversize before read, oversized chunked/no-length streaming abort, missing stream plus missing/malformed length without `text()` invocation, valid bounded-length fallback, and streamed success at exactly 16384 bytes. Use unique token/cloud-key/channel/response-key/error markers and assert none appears in error strings, redacted state, persisted settings, console spies, or exported status data. Assert the cloud key appears only in the required captured derive body, disappears after clear, and an old-generation key never reaches a newly accepted origin/token/policy.
  </action>
  <verify>
    <automated>cd /mnt/e/backup/code/personal/oss/kettu-goofcrypt &amp;&amp; npm test &amp;&amp; npm exec tsc -- --noEmit &amp;&amp; git diff --check</automated>
  </verify>
  <done>The client fails closed on unsupported Hermes transport surfaces, can safely consume only the exact authenticated Stage 2 API under the 270-second batch timeout and hard 16 KiB body boundary, validates every untrusted response before use, and clears the memory-only cloud key before any accepted configuration generation can change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the versioned ordered remote cache, revision coordinator, and lifecycle gates</name>
  <files>src/core/remoteKeycache.ts, src/cloud/remoteKdf.ts, src/cloud/session.ts, src/settings.ts, src/index.ts, tests/remoteKdfStage3.ts, tests/harness.ts</files>
  <behavior>
    - Persist one plain-JSON `remoteKeyCache` envelope with `version:1`, optional current revision/freshness metadata, and `channels[channelId] -> RemoteChannelKeySet[]`. Each set is exactly `{settingsRevision,keys,sendCapable}`; `keys` is contiguous server slot order. `MAX_REMOTE_REVISIONS_PER_CHANNEL=3` means one current plus at most two old decrypt-only sets.
    - Migration from the current manual-only Settings shape creates/sanitizes only `remoteKeyCache` and leaves `keys`, passwords, chosenIndex, cover, mark, imported keys, probe state, and all unrelated properties byte/deep-equal. Unknown/corrupt remote versions fail closed by replacing/quarantining only remote state; they never make a malformed key readable or damage the manual cache.
    - Every remote key is revalidated with canonical decode/re-encode equality and exactly 32 decoded bytes before persistence and again before return from a cache accessor. Invalid channel/revision/key/set/extras are rejected or removed before use. The cache never calls `passwordId`, accepts raw passwords, or re-keys entries by password-derived material.
    - Installing a strict derive response first marks all cached sets across every channel non-send-capable if the authoritative revision changed, then replaces/de-duplicates the target channel's matching revision at index zero, preserves exact key order, marks only that head current/send-capable, and truncates to three newest sets. Accessors separately expose current send keys and newest-to-oldest decrypt sets for Stage 4; callers cannot mutate stored arrays.
    - Applying a strict revision response updates the last successful check time and global revision. A changed revision immediately demotes every cached set across all channels; an unchanged revision preserves current send capability. Failed checks do not alter the timestamp/revision/cache or readiness. Clearing the remote cache removes only the remote envelope's channels/revision metadata and never clears manual keys/config unless the separate credential control is invoked.
    - Freeze `REMOTE_REVISION_TTL_MS=300000`. A configured on-load refresh is forced once per plugin load, explicit check is always forced, and `ensureRemoteRevisionFresh()` coalesces concurrent calls and skips network only while the last successful check is within TTL. This exported function is the future Stage 4 conservative pre-send gate; Stage 3 does not call it per message or import it from send/Flux.
    - The coordinator owns monotonic `configGeneration`, `mutationEpoch`, `nextRequestOrder`, and `lastAppliedOrder`. Every derive/revision captures its configuration generation and request order. A revision may mutate only under the same configuration and when its order is newer than the last applied operation; applying it advances the order and mutation epoch. A derive additionally captures mutation epoch and starting authoritative revision and may install/verify only if configuration, epoch, and current authoritative revision still exactly match those snapshots and its order is not stale. Every applied mutation advances the epoch/order, so a late response can never undo a newer applied mutation.
    - `refreshRemoteChannel(channelId)` requires valid config/token plus the current in-memory cloud key and coalesces at most one in-flight derive per channel. It calls derive, passes the snapshot/order commit gate, strictly installs keys, and only then marks the current configuration/revision verified/ready. Revision GET, persisted cache hits, config presence, token-only state, or a response rejected as stale never verifies. Any cloud-key/config change, authoritative revision change, failed/stale install, cache clear, or shutdown clears verification.
    - Initialization occurs after `initSettings`/manual `initKeyCache`, migrates remote state, initializes the client/coordinator, and starts a caught non-blocking load revision check when configured. Shutdown first closes/aborts the coordinator and ignores late completions, then clears pending refreshes, cloud-key and verified state while retaining valid persisted channel keys.
  </behavior>
  <action>
    Write migration/cache/coordinator tests before implementation. Model realistic pre-Stage3 storage containing manual `keys` and passwords, initialize remote storage, mutate/clear/refresh it, and assert the original manual subtree and unrelated settings remain deeply identical. Include null-hostile proxy coverage: never assign `null`; use a complete plain object replacement or `undefined`/delete for optional fields so Kettu cannot attempt `new Proxy(null)`.

    Implement `remoteKeycache.ts` independently from the existing manual `keycache.ts`. Define the v1 persisted interfaces, fixed retention cap, strict plain-object/exact-key validators, revision/key helpers, immutable-copy accessors, migration, install, demote, freshness, counts, and remote-only clear. Use existing `fromBase64`/`toBase64` and the frozen revision/key parsers rather than `Buffer`/`TextEncoder`. Do not merge remote keys into `settings.keys` and do not add remote data to desktop key import/export shapes.

    Implement `remoteKdf.ts` as the only orchestration layer that combines persisted configuration, client, memory-only session, and remote cache. Keep a bounded single in-flight revision promise, a per-channel derive-promise map, closed/configuration generations, and the shared mutation order/epoch state. Allocate request order before dispatch. Revision commit checks configuration plus `order > lastAppliedOrder`; derive commit checks configuration, captured epoch, captured starting revision, and non-stale order before any cache or readiness write. Every successful cache mutation advances `lastAppliedOrder` and `mutationEpoch`; stale completions return a stable non-reflective result/code, clear verification where required, and make no partial write. `refreshRemoteRevision(force)` and `ensureRemoteRevisionFresh()` never log. `refreshRemoteChannel` validates decimal channel before network, obtains the key only for the immediate call, joins the same channel's in-flight operation, and strictly stores only after the commit gate passes.

    Wire `index.ts` after settings/manual-cache initialization: migrate/init the remote coordinator and launch exactly one fire-and-forget load refresh whose rejection is consumed into redacted stable status, never `safe()` logging with caught network detail. On unload, call `shutdownRemoteKdf()` before clearing other volatile state. Do not add imports or edits in `flux.ts`, `send.ts`, `decrypt.ts`, or `encrypt.ts`.

    Tests must cover absent/manual-only migration, valid restart round-trip, corrupt/unknown cache recovery, canonical and 31/33-byte rejection before write/read, contiguous slot preservation, same-revision replacement, three-set retention, multi-channel global demotion, old decrypt ordering, remote-only clear, readiness truth table, config/key/revision invalidation, on-load force, explicit force, TTL boundary at 299999/300000 ms, concurrent revision coalescing, per-channel derive coalescing, failure timestamp preservation, unload abort, and ignored late completion. Use deferred fake responses to prove both completion orders for derive-vs-revision and derive-vs-derive: a revision applied after an older derive demotes it; a revision applied first makes the old derive commit fail; the first applied derive advances the epoch so the competing derive cannot overwrite/cache/verify; same-channel callers share one request. Repeat every race across configuration change and unload and assert no stale response resurrects send capability/readiness. Explicit channel refresh after settlement still invokes the server again while persistent channel keys remain only a local cache.
  </action>
  <verify>
    <automated>cd /mnt/e/backup/code/personal/oss/kettu-goofcrypt &amp;&amp; npm test &amp;&amp; npm exec tsc -- --noEmit &amp;&amp; git diff --exit-code -- src/discord/flux.ts src/discord/send.ts src/core/decrypt.ts src/core/encrypt.ts &amp;&amp; git diff --check</automated>
  </verify>
  <done>The current manual store migrates losslessly beside a strict bounded remote cache; only mutation-epoch/order-approved validated current keys can send, adversarial late derive/revision responses cannot resurrect stale cache or readiness, old revisions remain bounded decrypt-only, and unload removes all transient universal-secret state without wiring message paths early.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose secret-safe remote setup, refresh, status, clear, and compatibility gates</name>
  <files>src/ui/Settings.tsx, src/discord/commands.ts, src/cloud/remoteKdf.ts, src/index.ts, tests/remoteKdfStage3.ts, tests/harness.ts, docs/REMOTE_KDF_MOBILE_TRANSPORT.md, README.md</files>
  <behavior>
    - The settings screen keeps the existing manual password/key-import section intact and adds a clearly separate Remote KDF setup section. Host and explicit localhost-development policy are visible; the stored token is represented only as configured/not configured; token and cloud-key fields use masked secure entry, start blank, and clear local React state immediately after import/set.
    - Saving remote configuration imports an existing raw token after strict validation and warns accurately that the revocable token and channel keys live in plaintext Kettu storage while the cloud encryption key is session-only and must be re-entered after restart. There is no OAuth callback, fake vault, cloud-key persistence, or setting that claims ordinary plugin storage is secure.
    - UI actions provide: set/clear session cloud key, verify and refresh the current Discord channel through derive, force a revision check, show redacted status, clear only remote channel keys, and forget the stored token/config. A missing current channel produces a stable safe message and no request. Destructive actions identify whether credentials or cache are affected and never silently touch manual passwords/keys.
    - `/encrypt` adds `remote-status`, `remote-refresh`, `remote-check`, and `remote-clear` choices. It accepts no token/cloud-key/host command argument because command values and bot replies are copyable; refresh uses only the already-held session key and current channel. Existing manual `on/off/toggle/cycle/set/import/bench/diag` choices and behavior remain unchanged.
    - Remote status/replies show only configured, runtime-supported/bounding-mode category, session-key-present, verified-ready, revision-known/fresh, cached-channel/set counts, fixed TTL/retention, and a stable last code. `REMOTE_UNSUPPORTED` is actionable and cannot mark ready or trigger network. Status never displays origin, token, cloud key, channel ID, exact revision, returned key, response body, or caught message.
    - The UI and commands do not expose a remote message-mode toggle in Stage 3. They explicitly describe the manual pipeline as still active and remote setup/cache as prepared for Stage 4; therefore a user cannot select remote and unknowingly have current send/Flux hooks fall back to passwords/local Argon.
    - README documents existing-token import, strict HTTPS/localhost rules, token/plaintext-cache limitations, session-only key/unload/config-change behavior, runtime capability failure, current/old revision retention, explicit clear semantics, no `/v1/save`, and the Stage 3/Stage 4 boundary without claiming remote cold-path operation is already active. A dedicated mobile transport decision/checklist records that real-device Kettu redirect-error and AbortController cancellation must be verified in Stage 5; it records no unproven success claim and authorizes no permissive fallback.
  </behavior>
  <action>
    Extend the UI's small `Input` helper with `secureTextEntry`/placeholder support. Keep secret fields controlled by ephemeral component state only, never initialize them from persisted token/key values, and clear them after use or unmount. Route all async actions through stable code-to-safe-message helpers and disable/reject duplicate button actions through the coordinator's coalescing rather than creating extra requests. Do not pass caught errors to `showToast`, `vendetta.logger`, or bot replies.

    Add remote command choices to the existing single action picker. Keep token/key import exclusively in the masked settings screen; never add slash options for universal secrets. `remote-refresh` derives only for `ctx.channel.id`, `remote-check` forces revision GET, `remote-clear` clears the remote cache but not credentials/manual state, and `remote-status` uses one shared redacted formatter also used by the UI. If a request is already active, surface a bounded stable busy state or join it; do not launch a second derive.

    Add focused checks for redacted status/error formatting including `REMOTE_UNSUPPORTED`, missing-channel/capability no-fetch, UI-facing configuration setters, old-key clearing across every accepted config/forget transition, remote-only clear, and unique secret markers absent from every status/toast/reply/logger/debug/export string. Where React rendering is impractical in the custom harness, keep validation, action, and formatter logic in pure exported cloud functions and test those directly; retain `npm run build` as the real JSX/Hermes integration gate. Add a source audit in verification to prove no `/v1/` path exists in `src/cloud`, no cloud-key/token log/reflection was added, and the Stage 4 hook files have no diff.

    Create `docs/REMOTE_KDF_MOBILE_TRANSPORT.md` with the exact Stage 3 feature-detection contract, streaming-vs-bounded-length response modes, no-fallback rule, and a pending Stage 5 real-device checklist: prove Kettu/Hermes `redirect:"error"` does not follow a controlled 307/308 secret-bearing request, timeout/unload AbortController cancels fetch/body reading, and supported response mode enforces 16 KiB. Mark these device items pending rather than blocking this autonomous implementation. Update README only after behavior is implemented and keep it explicit that this stage provisions and verifies remote state but does not yet alter live message cold paths. Re-run the unchanged stegcloak-rs interop harness and the server v2 suite so UX/storage work cannot drift the frozen wire contract.
  </action>
  <verify>
    <automated>cd /mnt/e/backup/code/personal/oss/kettu-goofcrypt &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm exec tsc -- --noEmit &amp;&amp; git diff --check &amp;&amp; cmp tests/fixtures/remoteKdf/argon2id-v1.json ../goofcord-cloudserver/test/fixtures/remoteKdf/argon2id-v1.json &amp;&amp; test -z "$(git -C ../stegcloak-rs/GoofCord status --short)" &amp;&amp; cd ../goofcord-cloudserver &amp;&amp; bun run typecheck &amp;&amp; bun test test/kdf/contracts.test.ts test/integration/v2.test.ts</automated>
  </verify>
  <done>Users can safely import/configure, verify, inspect, refresh, and clear remote state without exposing universal secrets or damaging manual mode; unsupported Kettu transport surfaces fail closed and are visible only as a stable code, the Stage 5 real-device redirect/abort gate is recorded, and live cold-message behavior remains cleanly deferred to Stage 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pasted host/token -> persisted configuration | Origin, development downgrade flag, and existing raw token are user-controlled and must be validated before any secret-bearing request |
| session cloud key -> HTTP request | The universal key may exist only in memory and the exact derive body; redirects, logs, status, storage, and lifecycle races must not copy it elsewhere |
| Kettu/Hermes globals -> transport boundary | DOM type declarations and Node mocks are not runtime authority; missing/partial URL/fetch/abort/response behavior must fail closed before secrets move |
| remote server -> strict mobile parser | Status, content type/length, JSON shape, error mapping, revisions, slots, and key bytes remain untrusted until exact validation |
| strict response -> persistent cache | Only canonical 32-byte ordered keys under a valid revision may cross into versioned plugin storage |
| revision state -> future send eligibility | Stale revisions must be globally demoted before Stage 4 can treat any key as send-capable |
| manual cache -> remote cache migration | Adding remote state must not overwrite or reinterpret manual passwords, password IDs, or imported keys |
| plugin lifecycle -> transient secrets | Reload, configuration change, timeout, and unload must abort work and clear session key/readiness even when promises settle late |
| UI/commands/debug -> user-visible output | Copyable replies, toasts, logs, diagnostics, and exports may contain only redacted aggregate state and stable codes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-RKDF-S3-01 | Spoofing / information disclosure | remote origin/runtime transport | critical | mitigate | canonical HTTPS origin, explicit exact-loopback HTTP flag, fail-closed capability probe, redirect error/final-URL check, pending Stage 5 real-device redirect proof, no permissive fallback |
| T-RKDF-S3-02 | Information disclosure | cloud key/token | critical | mitigate | memory-only key, masked ephemeral input, raw token only in storage/header, redacted errors/status, abort/clear on unload |
| T-RKDF-S3-03 | Tampering | remote response/cache | critical | mitigate | exact Stage 1 parsers, status/code equality, response bound, canonical 32-byte revalidation before write/read |
| T-RKDF-S3-04 | Elevation / downgrade | key-source fallback | critical | mitigate | separate schemas/APIs, manual remains default, no remote toggle/hook wiring or silent fallback in Stage 3 |
| T-RKDF-S3-05 | Tampering | revision/derive races | critical | mitigate | shared config generation + mutation epoch + request order + starting revision commit gate, per-channel derive/revision coalescing, adversarial completion tests |
| T-RKDF-S3-06 | Denial of service | network/polling/cache growth | high | mitigate | explicit 270-second full-batch/15-second revision timeouts, abort, incremental/declared-length hard 16 KiB cap, five-minute TTL, bounded in-flight operations, three sets/channel |
| T-RKDF-S3-07 | Information disclosure | logs/UX/debug | high | mitigate | stable code-only errors and pure redacted formatter; secret-marker spies across status/toast/reply/logger/export paths |
| T-RKDF-S3-08 | Tampering | GoofCord settings | high | eliminate | client contains only literal `/v2/kdf/derive` and `/v2/kdf/revision`; tests/source audit prohibit every `/v1` write/read path |
| T-RKDF-S3-09 | Tampering | migration | high | mitigate | remote-only versioned envelope, fail-closed sanitization, deep-equality tests proving manual cache/settings preservation |
</threat_model>

<verification>
Run after all three tasks:

```bash
cd /mnt/e/backup/code/personal/oss/kettu-goofcrypt
npm test
npm run build
npm exec tsc -- --noEmit
git diff --check
cmp tests/fixtures/remoteKdf/argon2id-v1.json ../goofcord-cloudserver/test/fixtures/remoteKdf/argon2id-v1.json

# Stage 3 must not wire live message paths early or add a legacy cloud-settings call.
git diff --exit-code -- src/discord/flux.ts src/discord/send.ts src/core/decrypt.ts src/core/encrypt.ts
test -z "$(rg -n '/v1/(save|load|delete)' src/cloud || true)"
test -z "$(git -C ../stegcloak-rs/GoofCord status --short)"

cd ../goofcord-cloudserver
bun install --frozen-lockfile
bun run typecheck
bun test
git diff --check
```

Audit the mobile diff for any persisted `cloudEncryptionKey`, token/cloud-key command option, raw exception/body logging, response acceptance before exact parsing, noncanonical key write, password/passwordId in `remoteKeyCache`, unbounded revision history, redirectable Authorization request, `/v1` path, or remote-to-manual fallback. Confirm every accepted configuration/forget path aborts requests and calls `clearRemoteSession()` before generation change, the only persisted remote credential is the revocable existing raw token, and README/UI accurately call plugin storage plaintext.

Inspect the generated `site/index.js` guards and source graph: no class/generator/iterator-protocol syntax survives, no runtime `Buffer`/`TextEncoder` is introduced, the client/cache do not import local Argon or stegcloak, and `src/discord/flux.ts`/`send.ts` remain byte-unchanged. Confirm runtime use of URL/fetch/AbortController/Response is always behind the exception-safe capability gate; no XHR or permissive redirect fallback exists; server/mobile contract constants and the shared vector still agree. Confirm `docs/REMOTE_KDF_MOBILE_TRANSPORT.md` leaves controlled redirect and real abort cancellation explicitly pending for Stage 5 rather than asserting Node/build evidence proves device behavior.

Exercise tests proving exact raw Authorization and v2 paths; HTTPS/localhost/redirect rules; complete/missing/partial runtime capability matrices; stable `REMOTE_UNSUPPORTED`; exact 270000/15000 timeout budgets; fetch/body abort; declared oversize, streamed exact/oversize, and no-stream/no-length unread response behavior; all stable server/client codes; secret-marker absence; config-change/forget/unload key clearing; lossless manual migration; canonical 32-byte gates; ordered current plus two old revisions; global stale demotion; both completion orders of derive-vs-revision and derive-vs-derive under configuration generation/mutation epoch/request order; load/explicit/TTL polling; readiness only after a commit-approved strict derive; and remote-only clear/status/refresh behavior.
</verification>

<success_criteria>
- A configured Kettu client calls only the frozen authenticated v2 derive/revision endpoints after a fail-closed runtime transport probe, with exact schemas, a 270-second full derive-batch/15-second revision lifetime, hard 16 KiB stream-or-bounded-length response handling, no redirect/downgrade fallback, strict stable errors including `REMOTE_UNSUPPORTED`, and canonical key validation.
- The cloud encryption key is never persisted or exposed, is cleared before every accepted origin/token/policy change, forget, replacement, or unload, can never reach a new configuration generation, and cannot mark remote ready until a successful strict derive passes its configuration-generation/mutation-epoch/starting-revision commit gate.
- Existing manual passwords and `settings.keys` survive migration unchanged beside a plain-JSON versioned remote cache that stores only ordered channel keys/revisions, keeps one send-capable current set plus two decrypt-only old sets, and demotes globally on revision change.
- Revision polling is bounded and deterministic: once on configured load, forced on explicit refresh, coalesced behind a five-minute future pre-send API, never per message, and never advances freshness/readiness on failure; shared request order and mutation epoch prevent any late derive/revision/derive completion from resurrecting stale send capability or readiness.
- UI and commands provide explicit redacted setup/refresh/status/clear controls without accepting or emitting universal secrets; no `/v1/save` call or fake secure-storage claim exists.
- Manual mode remains the operational default and no Stage 3 source touches live Flux/send/decrypt/encrypt cold paths. Stage 4 receives clear APIs for current send keys, ordered historical decrypt keys, revision freshness, and remote channel refresh without any silent fallback.
- Full mobile typecheck/build/interop tests, runtime-capability/body-bound/race tests, frozen fixture comparison, full server tests, diff hygiene, and clean GoofCord reference-tree gates pass; the remaining real-device redirect/abort check is recorded explicitly for Stage 5.
</success_criteria>

<output>
Create `.planning/quick/260718-s4o-stage-3-implement-kettu-remote-cloud-ses/260718-s4o-SUMMARY.md` after execution.
</output>
